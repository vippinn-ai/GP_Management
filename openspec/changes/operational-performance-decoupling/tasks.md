# Tasks and evidence

Each completed item records commit SHA, command, timestamp, artifact path, and SHA-256.

## 1. Characterize

- [x] Freeze exact production-equivalent source SHA and staging configuration. Evidence: base `c0e0d2133f39372aac43e669c05e8c27ad5a255c`.
- [x] Trace hop/reject/continuation/startup/realtime/export paths. Evidence: `proposal.md`, `design.md`, independent first-phase reports.
- [x] Add v1 compatibility and v2 forbidden-`app_state` contract tests. Evidence: `src/qa/operationalLifecycleV2SqlContract.test.ts`.
- [x] Capture build, lint, tests, entry chunks, restore request count, and latency baseline. Evidence: local build/test output and measured baseline in `proposal.md`.

## 2. Lifecycle SQL

- [x] Add least-privilege `operational_mutations` and normalized-only v2 functions. Evidence pending independent review and staging install.
- [x] Harden new-tab continuation-source locking and actor attribution. Evidence pending independent review and staging races.
- [x] Add database-identity-bound installer, exact source/artifact/body/grant verification, guarded rollback capture, and transactional proof tooling. Evidence pending staging execution.
- [x] Add a narrow, preflight-bound reinstall path for corrections to already-installed v2 functions, preserving exact owner/ACL state and producing an exact definition rollback. Evidence pending independent review and staging execution.
- [ ] Prove `app_state` data hash/version/timestamp/updater invariance.

## 3. Frontend lifecycle

- [x] Add default-off v2 flag and fail-closed dependencies.
- [x] Route only target kinds to v2; send intent-only payloads.
- [x] Hydrate changed normalized rows before critical acknowledgement.
- [x] Do not reapply stale client snapshots after canonical v2 confirmation.
- [x] Make continuation-bearing tab flows confirmation-based.
- [x] Serialize realtime hydration completion.

## 4. Frontend performance

- [x] Demand-load XLSX/PDF libraries with popup-safe behavior.
- [x] Add subscription-first buffering/catch-up before writes are enabled.
- [x] Defer bill/report/audit/customer/stock-movement/expense-administration history until after safe interaction or its owning screen; preserve loaded noncritical slices across reconnect and explicitly refresh dashboard context.
- [x] Add named critical-bootstrap marks and a PII-free, immutable 30-cold-load Playwright harness for request/byte/measured dependency-depth, LCP, CLS, React commit, and safe-interactive evidence. Baseline and candidate are bound to exact bundle and dataset hashes.
- [x] Add a guarded PII-free staging scale-fixture builder, full production-count normalized rows, representative compatibility collections at production byte size, exact workload-shape identity, identity-only private registry, rollback-only proof, base-plus-auxiliary identity chain, trigger suppression/restoration, clean source/commit bindings, and exact pre-functional cleanup path after the live snapshot proved staging was below production scale. Superseded proof-a and proof-b packages were never executed. Proof-c was executed exactly once and failed before its workload because Monaco preserved CRLF inside the function body and Supabase default privileges added `service_role`; the transaction rolled back and an independent read-only postflight exactly matched the preflight. Proof-d then passed its rollback-only proof and applied the expected production-scale fixture, but the postapply verifier correctly stopped because the standalone read-only SQL still emitted raw CRLF-sensitive RPC hashes. No baseline or performance test ran. The exact cleanup passed and restored the complete preflight identity. Proof-e passed its rollback-only gate, exact postrollback verification, one-shot apply, and manifest-bound scaled-dataset verification. Baseline identity `normops-20260913-1649-perf-baseline-e` then stopped locally before browser or staging-network work because the runner compared semantically equal workload-shape objects using order-sensitive `JSON.stringify`; the identity is permanently closed. Proof-e cleanup subsequently passed once: cleanup result SHA `c9e846bef31ceaf539e47926476afd955ccc9dbc5dd602850e6ff19b7586c5f3`, read-only postcleanup SHA `ccb284d7deb30fa8857dfd38e8d2b22323630bd0084793a5e24bc45a214ab33e`, and manifest-bound cleanup verification SHA `756db0012630fb67b699989865663017b86afd96c72a5a86bb85a30567407b29` prove exact original restoration, fixture/key/RPC absence, five clean floors, and no production write. The runner now uses strict semantic JSON-value equality, with executable regression coverage for reordered object keys and negative value/type/missing-key/extra-key/null-versus-absent/array-order cases. Fresh preflight and fixture/package lineage are required; proof-e, dataset-e, and the closed baseline identity cannot be reused.
- [x] Execute the fresh package-f scale proof, apply, and frozen legacy baseline after three independent gates. Proof verification SHA `6f83f3754e41405066df1b18f1c67b4ccf39e086c36c209eb95e412eb9ad373e` proves exact rollback restoration; apply verification SHA `8b82ab5aa9fbc065eb9c5b65a3afe053014a8aec78ef5675bb487f4209b851b7` proves the production-sized fixture at app-state version 736 with no production write. Dataset manifest SHA `1c87b1a3b127b3ede279422f8a6b91bab35dd6bc13f1540a45a299a88f81b85c` bound the 30-load legacy baseline `normops-20260913-2030-perf-baseline-f`, which passed with evidence SHA `3e70e5d971eb2092c39212c14a0d291af6657675de8f496d91c1ed29db72af4c`, p95 14,349 ms, maximum 16,948 ms, and critical API bytes p95 1,071,686. Candidate preflight then exposed that the verified install and fixture recorded the same `app_state.updated_at` instant with `+05:30` and `+00:00` offsets; the runner now compares timestamp instants while preserving exact equality for every other identity field, with positive and negative executable coverage.
- [x] Preserve the failed one-shot candidate identity `normops-20260913-2050-perf-candidate-f` without retry. Summary SHA `985dbe5d308b5359fd9a659fe003afb61c0f2428900e215abb6993bec596e63b` and evidence-manifest SHA `a1054cc3d5f5adbfa803ae5d1c8893287bb05964fbc887e88f0df056de359626` show that the deferred Inventory history stopped at PostgREST's 1,000-row response cap instead of the frozen dataset's 1,506-row shape. The normalized movement reader now uses bounded, exact-count, deterministic pagination with count-drift, overlap, early-end, and incomplete-result rejection; the performance assertion sums every successful page instead of assuming one uncapped response.
- [x] Preserve the failed one-shot candidate identity `normops-20260913-2140-perf-candidate-g` without retry. Summary SHA `949d0b171d515438bff0a361096a69293453657a8b8e3ad49163714018462ffc`, raw evidence SHA `ca18c08906d32649c2a8e169bd127ac82b839d0943eb7fead200647df84c259c`, and manifest SHA `6787f6675eabac63796b7127e2072a2aafce938e6f61aafe6032cedc4e2a7c47` prove complete 1,506-row Inventory pagination but expose mixed Node/browser cutoff and decoded-shell-byte telemetry defects plus genuine browser-ready/LCP/render misses. Metric version 2 uses browser marks and resource timings, hashed occurrence correlation, transferred shell bytes, event-granularity Inventory timing, LCP attribution, complete soft-gate reporting, and invalidates the semantically incomparable legacy baseline.
- [x] Remove root one-second render coupling (30-second display clock), reduce the shared logo asset from 226 KB to 61 KB without changing its visual identity, preload it before restore completion, statically select the profiling renderer, lazy-load non-dashboard panels, and render only the responsive Inventory catalog representation in use.
- [x] Correct metric-v2 to use one in-page visible-dashboard comparison boundary for both builds while keeping candidate application-safe timing separate; fail closed in both modes across request/resource/response timing, status, transfer, body, and JSON evidence; test responsive Inventory layouts; preload its chunk post-safe; and remove the serial normalized organization lookup by independently settling it beside the active profile without weakening inactive-user precedence or subscription-first catch-up. Activate application identity only after authoritative restore and gate/reschedule the persisted operational queue on complete ready state, with an App-level delayed-restore regression.
- [x] Preserve the valid metric-v2 baseline `normops-20260914-0149-perf-baseline-j` and failed one-shot candidate `normops-20260914-0157-perf-candidate-a` without retry. Baseline evidence SHA `567f397cbfc2aa11f26d744dc0d3bcc0e9fc11e661d9602a4d550c2a95ebfcf8`; candidate evidence SHA `fa96dabb0ece4b05967835565b3a49ff135c7b610c5467d194bd7a4c0a760a7b`. Candidate passed correctness, response evidence, exact `1,000 + 506` Inventory pagination, payload/bundle/CLS/max-commit/relative-improvement gates, but failed safe p95 `5,983.3 ms`, safe max `6,317.8 ms`, LCP p75 `4,764 ms`, Inventory commit p95 `20.1 ms`, and Inventory-history p95 `2,392.4 ms`.
- [x] Freeze the separate low-risk render/evidence unit at `9cbb4d0ab937749fdbac2ee4e884cbe88e4ca1f3`: demand-gate the client Inventory report, make its subview parent-controlled while resetting to Catalog on navigation away, move backend DNS/preconnect hints into mode-isolated document HTML, clear stale text-LCP resource attribution, and record finite per-page/per-load/p95/max Inventory bytes. Local evidence: 73 files/714 tests, 14-spec static Playwright gate, staging and production builds, zero lint errors and three pre-existing warnings. This unit does not claim the absolute startup gate and does not authorize another 30-load run.
- [x] Implement locally, still default-off and not installed: the atomic critical-bootstrap flag, authenticated-only `STABLE`/read-only RPC, 160,992-byte server/client ceiling, exact envelope/row/tenant mapper, explicit single-shot/manual-reset recovery, default-off static entry, read-only pre/postflights, install-time realtime-security rebinding, exact ACL/metadata rollback guards, CRLF-safe hashes, clean exact-SHA artifact binding, and focused security/zero-write contract tests.
- [x] Rebuild the atomic bootstrap after the first staging postflight correctly failed closed on an oversized response: retain the exact row contract, remove duplicated inventory/variant snapshots and QA metadata while preserving nullable-field fallbacks and all legacy operational compatibility JSON, verify client reconstruction, regenerate preflight-bound artifacts, obtain fresh independent sign-offs, install once, and pass the exact read-only postflight before any frontend deployment. Exact install package `normops-20260914-0828-a680adc-staging` ran once; immutable postflight SHA `e68e6284a6be1cbd5232343a18722197dfc3e5b2f30753e8a8f62b1ff9a6921f` reports 133,365 bytes, a 27,627-byte margin, exact function/security identity, and unchanged `app_state`.
- [ ] Add and independently approve the manifest-bound atomic-bootstrap postflight verifier and performance-runner bindings for both lifecycle and bootstrap database evidence before any frontend deployment. The pending exact-SHA gate includes raw run/source binding, source-commit SQL proof, exact actor/payload/count validation, active post-fixture/dataset identity correlation, and exact runner/config/spec harness hashes. Then produce one fresh metric-v3 legacy baseline and one zero-retry candidate run.
- [ ] Close the 14 September fixture rollover safely: preserve the blocked original cleanup unchanged; add and independently approve a dedicated rollover-cleanup builder/verifier that excludes only the three clock-derived shape counters while retaining all stored identity, security, trigger, floor, and exact-ID/count checks; run one rollback-only proof, one corrected cleanup, a fresh fixture lineage, and then new baseline/candidate performance identities.
- [x] Implement locally, still default-off and not deployed: the startup coordinator overlaps one dynamic App import with one shared attempt-scoped session/realtime preparation, preserves `SUBSCRIBED` before the single bootstrap RPC, filters/deduplicates buffered events, and assigns identity only after catch-up. Focused gateway/hook/startup tests include ordered duplicate suppression, the 1,001-event fail-closed boundary, and fresh success after pre-subscribe cancellation; the staging-mode build passes.
- [x] Close the independent-review blockers locally: bind readiness and hydration callbacks to a unique subscription generation (including same-callback remount coverage); validate values, closed tab-permission enums, nullability, timestamps, arrays, and nested/compatibility structures for every bootstrap collection before mapping; include inherited-role policy applicability and exact helper definition/body/owner/security/volatility/configuration/ACL evidence in preflight/install/generated postflight/standalone postflight; reject unauthorized helper grantees; and verify rollback against the exact prior definition hash as well as its normalized body and metadata. Evidence pending a new exact-SHA three-agent gate.
- [x] Add executable coverage requested by the second independent gate: same-callback hydration success/failure, delayed readiness success/failure, current-generation-only notification, every helper definition/security/configuration/ACL mutation, optional canonical PUBLIC execution, and exact rollback restoration of captured ACL and grant-option state. The new success case exposed and closed a deferred-teardown ordering defect by cancelling teardown before adopting a cached realtime-ready promise. Evidence pending a new exact-SHA three-agent gate.
- [ ] Obtain independent code/security/test/performance GO on the immutable atomic-bootstrap candidate before any SQL install, staging deployment, or fresh performance race.
- [ ] Replace candidate-i's invalid dependency-depth/LCP measurements with independently approved metric-v3 harness evidence: atomic one-RPC depth must fail closed on correlation/timing defects, Login LCP must settle before deliberate Inventory navigation while safe/post-navigation values remain diagnostic, and bootstrap fetch-to-first-byte/download phases must use completed exactly correlated Playwright timing. Preserve the immutable v123 restore receipt and require a fresh metric-v3 baseline before a new candidate.
- [x] Preserve the zero-retry metric-v3 baseline `normops-20260914-1752-perf-baseline-m3a` and candidate `normops-20260914-1800-perf-candidate-m3a`. Candidate passed safe maximum, RPC, catch-up, payload, shell, bundle, Inventory-history, one-RPC/depth, and deferred-read gates, but failed visible-dashboard p95 `2,725.9 ms` against `2,399.7 ms`, Login LCP p75 `2,800 ms` against `2,500 ms`, and Inventory commit p95 `19.5 ms` against `<16 ms`; staging was restored to immutable v123 after read-only integrity verification.
- [x] Implement the independently recommended local remediation without weakening subscription-first safety or thresholds: snapshot LCP attribution at observer delivery, record FCP/realtime-status/structured boundary-profiler evidence, publish Dashboard audit activity independently of financial history, limit its preview to three one-line entries, and page the complete Inventory catalog at 40 rows/cards with memoized lookups. Focused evidence: production-shape 112-item desktop/mobile pagination and full-set search coverage plus 92 passing performance/gateway/Dashboard/Inventory tests and a passing production build. Fresh exact-SHA independent review and same-harness baseline/candidate evidence remain required.
- [x] Preserve exact commit `4e9491453ced9433c62fbe41fecb6db6debc95de` as independently rejected and never deployed: all three reviewers identified a reversible pagination-context defect plus incomplete integrated/fail-closed evidence; code review additionally found that a synchronous `SUBSCRIBED` callback could precede the subscribe-call mark, and performance review found the staging Activity flag absent.
- [x] Correct the review blockers locally: commit page one on every search/archive transition and enumerate all 112 unique items across three pages, later-page action identity, full stock selector and report inputs; mark subscribe before invoking the helper with a synchronous-callback regression; validate and persist full status, FCP/LCP and `bp-app`/`bp-dashboard`/`bp-inventory` evidence; add an App-level delayed-history Activity/Show All test; and enable the ignored local staging Activity flag. Focused validation passes 6 files / 120 tests; full Vitest passes 83 files / 884 tests; production build, strict OpenSpec, the 14-spec static Playwright gate, and four-case Activity desktop/mobile discovery pass; lint remains zero errors with the same three pre-existing warnings. A new exact-SHA independent review and staging measurement remain required.

## 5. Gates

- [x] Add reusable zero-retry suites for exact 20-by-3 target sampling, 50-pair overlap proof, unit-sale/continuation consumers, lost-response/realtime recovery, five hop mutation races, and downstream receipt/mobile/logout parity. Evidence pending live staging execution.
- [x] Replacement-candidate local gate: lint and no-emit TypeScript across every one of the 14 selected operational Playwright specs, lint (0 errors, 4 pre-existing warnings), 63-file/614-test Vitest, production/staging builds, 34-case operational Playwright discovery across 14 files, one separately reconciled customer-profile parity case, and the separate 30-load performance case. Shared settled cleanup and its contract suite preserve primary failures and reject uncontained `finally` awaits. Evidence pending immutable candidate commit.
- [ ] Reviewer approves exact candidate SHA.
- [ ] Fail-closed staging preflight and flag-off SQL install pass.
- [ ] Functional, edge, security, idempotency, race, recovery, realtime, parity, and performance suites pass.
- [ ] Exact-ID cleanup and immutable postflight pass.
- [ ] Tester GO and deployed-definition reviewer GO are recorded.
- [ ] Production remains untouched pending fresh explicit approval.
