# Tasks

## Baseline

- [x] Preserve pre-change WIP in commit `63c2b8a`.
- [x] Create `codex/checkout-app-state-decoupling`.
- [x] Complete reproducible per-file review manifest and source-of-truth traceability matrix.
- [x] Resolve the current lint error; local lint has zero errors and five pre-existing warnings (no new warning gate).
- [x] Prepare the gated Release A staging installation/verification/rollback runbook.
- [x] Capture staging schema/function/grant/parity evidence.

## Release A: normalized reads

- [x] Load current-business-day payments and their older bills during normalized bootstrap.
- [x] Load recoverable unbilled hopped sessions.
- [x] Make normalized inventory authoritative in operational helpers.
- [x] Prove generic full-state saves remain blocked in normalized bootstrap.
- [x] Prove compact realtime closes/removes live entities and merges financial overlays by ID.
- [x] Complete scoped read coverage for downstream screens/actions, including historical Bill Register action hydration and normalized customer visit activity.

## Release B: financial v2

- [x] Add financial mutation idempotency schema and RLS.
- [x] Add checkout v2 and adjustment v2 RPCs without `app_state` access.
- [x] Add authenticated actor enforcement and deterministic row locking.
- [x] Add mutation-status reconciliation.
- [x] Add frontend v2 flag, stable mutation identity, and bounded recovery.
- [x] Preserve v1 and local mode.

## Verification

- [x] Local unit and characterization tests pass (39 files / 452 tests on 2026-08-25).
- [x] Add a reproducible staging-only Playwright harness with exact-host/project guards, ignored credentials, two independent contexts, zero retries, trace-disabled credential safety, compact RPC evidence, and failure-only screenshots/video.
- [x] Execute the Playwright pause-edit and hop/detach scenarios with staging credentials; retain their reported harness failures and reconcile the exact canonical RPC, observer, hard-refresh, cleanup, continuation, and Bill Register evidence without retrying checkout.
- [x] Restore normalized-ready report CSV/XLSX/PDF exports, prove keyset pagination beyond one backend page locally, and inspect all three generated formats on staging.
- [x] Execute the two-browser combo, variant, cigarette-pack, and reservation before/after-refresh Playwright matrix with exact RPC and cleanup evidence.
- [x] Run the final consolidated Release A staging Playwright matrix with five passes in one zero-retry run; preserve and reconcile the two earlier harness-only failures before correcting their synchronization assertions.
- [x] Pass the exact-definition reject-release transactional staging proof with authenticated/anonymous grant checks, in-transaction mutation coverage, rollback-to-savepoint restoration, zero residual fixtures, exact compatibility reconciliation, and independent evidence GO.
- [x] Persist the proven reject-release RPC definitions in staging, verify all six hashes/grants/security modes, deploy the corrected staging frontend, and prove the definition install changed no operational or compatibility data.
- [x] Repair the quarantined S1/S2 QA continuation link. The first guarded attempt failed closed and fully rolled back; the separately approved trigger-aware second attempt passed once, advanced compatibility v624 to v625, removed all three link representations, and passed exact independent postflight reconciliation.
- [x] Reconcile the separately authorized three-session multi-hop Playwright attempt without retry. Exactly one Rs 45 bill/payment/mutation/event was committed, all three sessions closed to that bill, and the floor returned empty; the intended two-command race was not submitted because of an interception lifecycle failure, so the product concurrency case remains open. Harden the reusable zero-retry harness locally and retain a fresh-authorization gate for any rerun.
- [x] Reconcile fresh authorized run `20260825104947` without retry. It stopped before interceptor installation or financial identity creation because the observer emitted authenticated REST reads but no POST RPC; guarded rejection released both prior continuation links, exact read-only evidence proved zero bill lines and an empty floor, and the reusable metadata collector was broadened locally. The multi-hop concurrency case remains open and another run requires separate approval.
- [x] Reconcile fresh authorized run `20260825114908` without retry. It stopped before interceptor installation or financial identity creation because the harness selected nonexistent `profiles.organization_id`; guarded rejection again released both prior links, exact evidence proved zero bill lines and an empty floor, and the schema-correct profile/organization-role preflight was fixed locally. The multi-hop concurrency case remains open and another run requires separate approval.
- [x] Reconcile fresh authorized run `20260825120115` without retry. It stopped before interceptor installation or financial identity creation because the harness incorrectly required the normalized-only primary session in compatibility `app_state`; source and database evidence proved only carried/hopped sessions belong there, guarded cleanup left zero bill lines and an empty floor, and the compatibility assertion was corrected locally. The multi-hop concurrency case remains open and another run requires separate approval.
- [x] Pass the fresh zero-retry three-session multi-hop double-bill race. Two distinct commands were submitted once, exactly one Rs 30 bill/payment/mutation/event committed with three Rs 10 session lines, the loser returned `session_not_billable` with null status, all three sessions closed/billed to the winner, actors matched, `app_state` remained unchanged across the financial race, both browsers reloaded Available, and independent read-only postflight found an empty floor.
- [x] Pass the dedicated receptionist/manager checkout-hop timing matrix. Runs `role_matrix_20260825_1457`, `role_matrix_20260825_162048`, and `role_matrix_remaining_20260825_165519` proved both checkout-first outcomes and receptionist checkout versus manager hop-first while exposing and correcting zero-total payment and delayed-alert harness assumptions. Final zero-retry run `role_matrix_final_20260825_2249` passed manager checkout versus receptionist hop-first and both simultaneous role directions. Every exact run was fully reconciled to an empty floor with correct actor, bill, payment, audit, event, mutation, and compatibility evidence; all run-specific profiles are inactive and credential files removed. All six ordinary-role direction/order combinations are now demonstrated.
- [x] Reconcile checkout-versus-standalone-settlement attempt `20260827053236` without retry. It stopped before either race interceptor or financial identity existed because the still-open session modal blocked a redundant dashboard-navigation click. The setup created one exact Rs 45 deferred bill with zero payments; guarded cleanup rejected the exact second session, and read-only postflight proved zero open sessions/tabs, the one expected compatibility increment, and no settlement effect. The redundant navigation was removed locally; the concurrency case remains open and another staging run requires separate approval.
- [x] Reconcile checkout-versus-standalone-settlement attempt `20260827054102` without retry. Both UI commands were captured once, but a pre-send whole-header assertion failed before deliberate concurrent submission. Failure teardown removed interception too early and the checkout's bounded ambiguous-response path subsequently committed one canonical checkout that also settled the setup bill; no deliberate standalone-adjustment submit occurred and no persisted adjustment result or effect was evidenced. Exact read-only reconciliation proves two Rs 30 bills, the two expected Rs 30 cash payments, authoritative admin attribution, zero open sessions/tabs, and unchanged compatibility version/hash `643` / `f0c3300bb4e4c024a994fb41aa1aa266fea4bc91ed498e987687cdecd6174208`. The interceptor now remains installed through request settlement and pre-race page quiescence, evidence captures mutation identities before later assertions, and attached errors are redacted. This is functional recovery evidence only; the concurrency case remains open and this authorization is consumed.
- [x] Close the checkout-versus-standalone-settlement functional subgate from reconciled zero-retry run `20260827115031`. The run submitted each captured UI command once; the standalone adjustment won, settled `BILL-20260827-004` with exactly one Rs 30 cash payment/audit/event, and the checkout lost with HTTP 400, a null mutation lookup, and no candidate bill. Exact before/after reconciliation proves authenticated-admin attribution, unchanged `app_state` version/hash across the financial race, identity-bound rejection of the unbilled losing session through cleanup `cleanup_20260827121000`, an empty floor, and retained `BILL-20260827-001` unchanged. Playwright remains reported failed because a later cleanup-eligibility assertion expected raw status `open` instead of normalized `active`; the functional/database result passed, the assertion is corrected, and no rerun was performed.
- [x] Close the checkout-versus-replacement shared-inventory subgate from fresh zero-retry run `replace-race-20260828074932`. Exactly one Playwright test captured and submitted the checkout and quantity-increasing replacement once each; both committed with three exact bills/payments/audits/events and three sale movements of -1, stock reached 0, original/replacement links and actors were exact, both tabs closed/billed, reservations released, and the financial race left `app_state` unchanged. The exact QA item archive alone advanced compatibility once, left an empty floor, and independent immutable postflight returned staging GO.
- [x] Close the checkout-versus-refund shared-inventory subgate from fresh zero-retry run `refund-race-20260828082130`. Exactly one Playwright test captured and submitted checkout and refund once each; both committed with three canonical mutations/events, two exact Rs 50 bills/payments, no fabricated refund payment, stock movements sale -1, sale -1, and reversal +1, final stock 1, exact refund reason/lifecycle actors/audits/tab links/reservation rows, and no extra movement. The financial race left `app_state` v661/hash unchanged; the exact QA item archive alone advanced compatibility to v662, left the fixture inactive and floor empty, and independent immutable postflight returned staging GO.
- [x] Close the checkout-versus-live-repeat-combo subgate from the combined zero-retry orderings `20260828-combo-race-010807`, `20260828-combo-race-rem2-012243`, and `20260828-combo-race-ui-fix-014747`. Checkout-first is retained honestly as a Playwright failure caused by a broad loser-alert locator seeing an unrelated hop reminder, while corrected immutable reconciliation deterministically proves the checkout winner and zero repeat effect. Combo-first and simultaneous prove the repeat winner and zero checkout effect with exact command cardinality, mutations/events/audits/actors/items/reservations/stock/session and `app_state` evidence; the simultaneous UI-fixed run passed. Identity-bound cleanup changed no financial or stock rows and restored an empty floor, and dedicated fixture cleanup archived the exact combo/item in order while preserving stock 19 with no new movement. Independent review returned staging GO without another browser run.
- [x] Close the checkout-versus-direct-session-item subgate from immutable zero-retry runs `session-item-race-202608280924`, `session-item-race-202608280936`, and `session-item-race-rem2-202608280950`. The first two runs each proved checkout-first as one canonical Rs 45 bill/payment/mutation/event with the stale item command rejected `session_not_open` and zero item effect; their later UI convergence failures were independently reconciled, identity-bound cleanup restored an empty floor, and exact temporary admins were deactivated. The final cost-saving remaining-two run passed Playwright once and mandatory database reconciliation: item-first and simultaneous each produced one exact item/reservation/audit/event, the stale checkout failed server-authoritative source-item validation with null mutation and zero bill/payment/financial event, both exact sessions were rejected afterward, stock returned to 3, the fixture was archived, `app_state` advanced only through acknowledged operational commands, and the floor was empty. The final temporary admin is inactive and its ignored credential file was removed. All three required orderings are now evidenced without reusing an ambiguous run identity.
- [x] Reconcile and clean partial customer-tab race run `tab-mut-race-20260828-1755` without retry. Only `add_item / checkout_first` executed: one canonical Rs 50 bill/payment/sale movement committed, the stale add returned `customer_tab_not_open` with zero operational effect, and the financial race left `app_state` unchanged. The later UI assertion failed because the observer correctly retained `1 conflict`; the harness now asserts and clears that expected loser conflict before Synced. Fresh reconciliation authorized identity-bound fixture archival, independent postflight passed with an empty floor and retained financial chain, and the temporary admin is inactive with its credential file removed. A separately reviewed fail-closed `remaining-eleven` selector bound runner, preflight, browser evidence, reconciliation, and postflight while excluding the proven case. At this checkpoint the other 11 orderings remained open; the subsequent task records their execution and closure.
- [x] Reconcile and clean remaining-eleven customer-tab attempt `tab-mut-rem11-20260829-1305` without retry. The global 120-second timeout occurred while preparing `remove_item / simultaneous`; seven earlier cases were fully reconciled and the eighth submitted no race command. Exact recovery proves two checkout winners and five operational winners with canonical rows/actors/effects and no duplicate financial or stock effects. Cleanup rejected only the prepared tab and archived the combo, then stopped on a deterministic stale-version HTTP 400 before item mutation. Purpose-built partial recovery authorized only one fresh item archive; that zero-retry continuation and mandatory postflight passed at compatibility v684 with stock unchanged at 62, empty floor, and all prior evidence preserved. The temporary admin is inactive and its credential file is removed. Including the prior run, 8/12 orderings were closed. The fail-closed `remaining-four` run `tab-mut-rem4-20260829-1338` then passed exactly `remove_item / simultaneous` plus all three `apply_combo` orderings: one checkout winner and three operational winners, exactly one Rs 50 bill/payment/-1 sale movement, correct distinct-actor attribution, null losing financial mutations, no duplicates, all tabs terminal, fixtures inactive, and an empty floor. Zero-action finalizer and independent postflight preserved compatibility v691/hash `2de2c4ab0d04f2e65a4031cb987a72bc32bd5cc34af5cbadcddf45786857eb55`; the temporary admin is inactive and credentials removed. All 12/12 customer-tab source-mutation orderings are closed.
- [x] Reconcile checkout-first from pending-write-off race `writeoff-race-20260829-1424` without retry. Checkout was the sole winner: two exact Rs 30 bills and two Rs 30 cash payments committed, the prior pending bill became fully settled, both sessions closed/billed, and the write-off returned `financial_adjustment_conflict` with null mutation and zero effect. Immutable reconciliation revision `audit-normalization-1` is `partial` with `failures: []`, exact actors/events/audits, zero stock, empty floor, unchanged `app_state` v691/hash, and no cleanup candidate. The later UI failure was a harness mismatch between financial remote-error and operational-conflict presentation; the reusable assertion is corrected locally. The temporary admin is inactive and credentials removed. `writeoff_first` and `simultaneous` remain open, so this does not close the write-off subgate.
- [x] Reconcile write-off-first from fail-closed remaining-two run `writeoff-rem2-20260829-1445` without retry. Write-off voided exact Rs 30 `BILL-20260829-002` with its canonical mutation/event/audit under the distinct adjustment actor; checkout returned `settlement_conflict` with null mutation and zero bill/payment/stock effect. The run stopped afterward because the losing checkout modal blocked the remote-error Dismiss click, so `simultaneous` did not start. Actor-bound read-only revision `actor-bound-1` returned `partial`, `failures: []` and authorized exactly one cleanup; `writeoff-rem2-cleanup-20260829-1450` rejected only that session, and mandatory postflight proved an empty floor, unchanged financial evidence, and only the rejection compatibility increment to v692. The temporary admin is inactive and credentials removed. Both deterministic winner directions are now closed; only `simultaneous` remains open.
- [x] Close the checkout-versus-pending-write-off subgate across `checkout_first`, `writeoff_first`, and `simultaneous`. The simultaneous-only run `writeoff-sim-20260829-1457` produced one canonical write-off winner for Rs 30 `BILL-20260829-003` under the distinct adjustment actor, while checkout returned `settlement_conflict` with a null mutation and zero bill/payment/stock effect. Its post-outcome Playwright failure was confined to modal sequencing after both server responses; immutable reconciliation returned `partial`, `failures: []` and authorized only the exact losing-session rejection. Cleanup `writeoff-sim-cleanup-20260829-1957` and mandatory postflight returned `passed`, `failures: []`, an empty floor, no cleanup candidates, and compatibility advanced only through that acknowledged rejection to v693. The local reusable UI harness now closes both the losing financial modal and managed-session dialog before dismissing the remote-error banner. The temporary admin is inactive and credentials removed. Combined with the two earlier exact runs, all three orderings are closed without a race rerun.
- [x] Close the checkout-versus-issued-bill-void subgate from fresh zero-retry run `void-race-20260829-2027`. One exact one-worker Playwright race passed, and immutable reconciliation proved original Rs 50 bill voided by actor B, checkout Rs 50 bill issued by actor A, exactly two retained Rs 50 cash payments, two bill lines, exact `-1/-1/+1` stock movements, final stock 1, exact void audit/event/reason, both tabs closed/billed, empty floor, and financial `app_state` unchanged. Exact fixture archival alone advanced compatibility to v695. No recovery cleanup was needed. Independent review issued GO; temporary admin `void-race-admin-20260829-2026` is inactive, its credential file is removed, no password was printed, and immutable deactivation SHA-256 is `8f0003654445fcf909edfcc822422f8a04611b854b57fdfd9675e2717a31feb5`.
- [x] SQL transaction/security/idempotency/concurrency tests pass. Anonymous, wrong-organization, actor-spoof, malformed, inactive-user, rollback, authenticated financial/admin role boundaries, same/different-mutation, over-settlement, bill-number collision, exact- and over-capacity limited-stock contention, both admin inventory metadata orderings, both checkout/reject orderings, all authoritative-admin and ordinary-role checkout/hop direction/order combinations, single- and multi-hop double-bill races, standalone settlement, replacement, refund, issued-bill void, repeat-session-combo, direct session-item contention, all twelve customer-tab item/combo orderings, and all three pending-write-off orderings pass. See `remaining-release-b-gap-audit-2026-08-28.md`.
- [x] Implement and independently review the reusable payments/previous-dues Phase A harness. Exact `upi`, `split`, and `partial_previous_dues` modes bind package runner, immutable preflight/verify, captured-not-submitted and immediate-response checkpoints, one-test Playwright execution, immediate financial `app_state` windows, actor/run-scoped reconciliation, SHA-bound identity cleanup, and mandatory cleanup postflight. Syntax, TypeScript, focused contract 5/5, full regression 47 files/499 tests, lint, build, and exact one-test discoveries pass locally.
- [x] Close the UPI payment subcase from fresh zero-retry run `paymatrix-upi-20260830-1257`: one canonical Rs 30 issued bill, one Rs 30 UPI payment, one session line, exact mutation/event/audits/actor, empty floor, and compatibility v695/hash unchanged. Independent review returned GO; no cleanup was needed.
- [x] Reconcile and clean both pre-submit split attempts without retry. `paymatrix-split-20260830-1301` created no checkout command or financial row; exact cleanup `paymatrix-split-cleanup-20260830-1305` rejected its sole QA session and advanced compatibility only through that rejection to v696/hash `74af7cf837c2087b84edd75cce78e551ff81d9f1537fbcef4d7a09d0c253ef0b`. The fresh `paymatrix-split2-20260830-1310` attempt also stopped before command capture/submission: retained UI evidence proved checkout had captured the old 13:08 start while the operator edit was being acknowledged as 12:58, making the 13:07 end produce a zero preview. Immutable reconciliation had no integrity failures, zero financial rows, and exactly one cleanup candidate. SHA-bound cleanup `paymatrix-split2-cleanup-20260830-1320` rejected only session `session-ba20ce3a-2f2b-4220-a7ca-f4242004dfb7`; mandatory postflight passed with an empty floor, zero financial effects, exact rejection actor/event/audit, and compatibility advanced once to v697/hash `792657af206500569a1821962fbd4ac8f6766444dc4c2b8504f9eafd98a32b12`. The harness now awaits the exact successful `save_live_session_details` response, asserts the edited start/end values, requires a positive rendered total, fills UPI as `total - 10`, and verifies exact Cash Rs 10/UPI values before interception. Fresh split preflight `paymatrix-split3-20260830-1330` passed; a new split mutation remains separately gated.
- [x] Close the split-payment subcase from fresh zero-retry run `paymatrix-split3-20260830-1330`. Immutable reconciliation SHA-256 `cac10df9ffe58a0ba551e299604d3f01a80414592b65c4643f415c4b75346ed8` passed with no failures or integrity issues: exactly one Rs 30 issued split bill and 9-minute linked session line, exact Rs 10 cash plus Rs 20 UPI payments, one canonical mutation/event, two server-canonical audits, correct actor, terminal closed/billed session, empty floor, zero discounts/stock, and unchanged compatibility v697/hash. Independent review returned GO/CLOSED; no cleanup or rerun is needed.
- [x] Reconcile and clean partial-previous-dues attempt `paymatrix-dues1-20260830-1340` without retry. The source checkout committed canonically as pending/deferred `BILL-20260830-003` for Rs 30 due. The second session stopped before command preparation because the readiness locator matched both exact `Total` and `Total Payable Today`. Reconciliation was unambiguous with no integrity failures and one exact open session. SHA-bound cleanup `paymatrix-dues1-cleanup-20260830-1350` rejected only that second session; mandatory postflight SHA-256 `647fa62085e2112b77b5faebf1d61716a307bc21eca85cf5808538dac2a3fc0` passed, preserved the pending source bill and canonical mutation exactly, left the floor empty, and advanced compatibility only once to v698/hash `4f30d39dbe36f52518dbb920b7dfc077e88bc6adf958dc484713752d744e3f8e`. The reusable locator now anchors to the exact `Total` label; TypeScript, focused contracts, and exact discovery pass. A fresh independently reviewed run remains required.
- [x] Reconcile and clean partial-previous-dues attempt `paymatrix-dues2-20260830-1400` without retry. Canonical pending/deferred `BILL-20260830-004` for Rs 30 due committed as the source, while the current checkout stopped before command preparation when the controlled Rs 20 upfront field had not yet committed/enabled its mode selector. Reconciliation was unambiguous with no integrity failures. SHA-bound cleanup `paymatrix-dues2-cleanup-20260830-1410` rejected only the exact second session; postflight SHA-256 `de22e1e01517e4043dc0b01e26661f5867aba855f5a1dc6659f9a7ef9eebe4b5` passed, preserved the source bill and canonical financial evidence, emptied the floor, and advanced compatibility once to v699/hash `695afca0f8047654c261ed7205f42b83c87cc465e3cc32704477d024535b8791`. The harness now blurs and verifies the Rs 20 controlled value, waits for the exact `Upfront Mode` combobox to become enabled, and verifies UPI selection before continuing. Local TypeScript, focused contract 5/5, and discovery pass. Independent review closed the cleanup and approved the correction; a fresh independently reviewed run remains required.
- [x] Reconcile and clean partial-previous-dues attempt `paymatrix-dues3-20260830-1430` without retry. The source committed canonically as pending/deferred `BILL-20260830-005` for Rs 30 due; the current command was never prepared because the label-based `Previous Dues Payment` selector did not bind stably to the rendered combobox. Reconciliation had no ambiguity or integrity failure. Cleanup `paymatrix-dues3-cleanup-20260830-1440` rejected only the exact second session; mandatory postflight SHA-256 `120736240d05f67c1e248a48e3f06940e4a591b6c1fcfd1f8fc677a5b231a73d` passed, preserved the source financial chain, emptied the floor, and advanced compatibility once to v700/hash `ce90bc58c6887be3561e8e784c026903adca1d8ea7791fc92be49fae895e431c`. The remaining previous-dues combobox/textboxes now use exact accessible roles with selected-value, blur, and amount assertions. TypeScript, focused contract 5/5, and discovery pass; independent review is required before another fresh run.
- [x] Close the partial-collection plus previous-dues subcase from zero-retry run `paymatrix-dues4-20260830-1500`. Immutable reconciliation SHA-256 `f4053ab7a689db04dd2f76715f6b6bee6b0be8680eca2e19fe1146edb17f0726` passed with no failures or integrity issues. Source `BILL-20260830-006` moved from Rs 30 pending to fully settled through exact Rs 10 cash plus Rs 20 UPI payments linked to checkout `BILL-20260830-007`; the current Rs 30 deferred bill collected Rs 20 upfront UPI and remains correctly pending with Rs 10 due. Both 9-minute sessions are closed/billed, two canonical mutations/events and seven server-canonical audits have the correct actor, the floor is empty, stock/discounts are unchanged, and financial compatibility remains v700/hash unchanged. Independent review returned GO/CLOSED; no cleanup or rerun is needed.
- [x] Implement the reusable pricing/zero Phase B harness with exact discount/rounding, LTP zero, discount-driven zero, and true zero-price guard modes; immutable preflight/request/response/failure evidence; actor-bound reconciliation; SHA-bound identity cleanup; and mandatory cleanup postflight. Syntax, TypeScript, focused contracts, full regression 48 files/506 tests, production build, and one-test Playwright discovery pass.
- [x] Correct the acknowledged live-detail/realtime convergence race discovered by `pricing-positive2-20260830-1435`. Absolute `saveLiveSessionDetails` and `saveLiveCustomerTabDetails` mutations are re-applied after acknowledgement, while additive item mutations remain excluded. The fix is unit-pinned and deployed only to staging as Worker version `077da7d6-9f7c-4ca5-9ef5-51bed632615c`, bundle SHA-256 `817e4613aa821f94ab13009365c2a19e048162d0b51749031df77bb1e294932a`. Fresh run `pricing-positive3-20260830-1450` proved the edited start survives acknowledgement, realtime, hard reload, and checkout.
- [x] Close positive discounts plus rounding from `pricing-positive3-20260830-1450`: Rs 33.33 positive subtotal, exact Rs 1 line discount, Rs 1 bill discount, Rs -0.33 roundoff, and canonical Rs 31 cash `BILL-20260830-008`. Corrected immutable reconciliation SHA-256 `77e2d67f437920fba73bcebac38ee1ea427fbfb0045c80bbd7d66007641e1ba9` passed with exact rows/actors/events/audits, empty floor, and unchanged financial compatibility.
- [x] Close LTP zero and manual discount-driven zero. `pricing-ltp2-20260830-1520` reconciliation SHA-256 `3d2e281fbdbb971f6dc89845c1baa893cc42e6564c0cd8f01748aaaa2683d452` proved the exact automatic LTP waiver, zero total/due/paid, and zero payments. `pricing-billzero2-20260830-1545` reconciliation SHA-256 `24451f0c933206de8e8b40ed8c489e4226e4a6539dde275860169116e279d6ba` proved a positive subtotal removed by exact 100% bill discount, zero total/due/paid, and zero payments. Both preserved `app_state` and left the floor empty.
- [x] Close true zero-price rejection from `pricing-truezero-20260830-1550`. The UI issued no financial request; reconciliation SHA-256 `9c434bf94a3dc23a14a422fb580aa3dcedbed427496b8a16c3083e1b00c9b3cc` found no integrity/completion failure or ambiguity and zero financial effects. Identity-bound cleanup `pricing-truezero-cleanup-20260830-1555` rejected only the QA session and archived only the exact item; postflight SHA-256 `4342aceff0ab43ed2926196cb35160a3ec5df90cdd0560ee35da540e3c4cfa52` passed with inactive fixture, empty floor, exact actor/lifecycle, and compatibility v706/hash `dbb4383c388109c19da67b54afbe19724dc7179bbaa07cc9e01f77dae87cf26a`.
- [x] Obtain independent Phase B closeout. The independent agent verified all four fresh identities and SHA-256 values, confirmed stopped attempts were excluded and safely cleaned without retry, and proved the live-detail correction through acknowledgement, realtime convergence, hard reload, and checkout. Verdict: GO/CLOSED for pricing/zero only; no rerun or additional cleanup required.
- [x] Close replacement/downstream parity from source run `replacement-parity-20260830-1542` without repeating its financial mutations. Corrected actor-bound read-only reanalysis SHA-256 `2879af27e847c5b321037f335c5494aac4b8d9dd4749518717eaf65241a087e7` has zero integrity failures or ambiguities and proves exact Rs 100 original/replaced plus Rs 50 replacement bills, exact cash payments, quantity 2-to-1 lines, stock 5-to-3-to-4 through exact `-2/+1` movements, correct actor/audit/events/canonical mutation results, closed billed tab, empty floor, and unchanged financial `app_state` v710/hash. The reusable resume harness is hard-locked to that source/SHA, aborts every browser RPC except an explicit read-only allowlist, and passed under fresh identity `replacement-parity-resume-20260830-1852`; terminal SHA-256 `800c046d318d039e631676cd0a6fe1a106610d9e39d5dc869445ed7c84fe2908` proves two hard refreshes and exact Bill Register/receipt/payment, grouped Receivables, Analytics, Customer Profiles, and Inventory report parity with `blockedRpcRequests: []`. Earlier resume identities are immutable locator/DTO diagnostics and are not passing cases. Exact archive-only cleanup `replacement-parity-cleanup-20260830-1855` archived item `inventory-a25e5ce4-9af1-4d66-8a78-b667f1ede2b1` at stock 4; mandatory postflight SHA-256 `0c21bf301905fd559ef9d42e40e262be45dd98e94bfbc44eeb62f8b42d57c3d1` preserved all financial/event/audit/canonical evidence, left an empty floor, and advanced compatibility only once to v711. Independent review returned GO/CLOSED for this phase.
- [x] Two-browser staging test passes. Basic session/tab checkout, settlement, refund, replacement, realtime, refresh, payments/previous-dues, pricing/zero, carryover, and the final replacement/downstream parity phase are closed.
- [x] Performance thresholds pass on production-sized staging data. The SHA-bound corrected 50-case Arcade session/inventory evidence is retained, and fresh zero-retry mixed run `mixed-20260830-2004` adds partial previous-dues settlement, quantity-decreasing replacement, and simultaneous checkout-versus-repeat-combo contention. Aggregate SHA-256 `39ad4a4439fbbfcc3e7f4c62537200d0b76050b0e1ff46896978f840e1f07800` passed with database p95/max `127.985 ms`, contention-winner response `376.374 ms`, and representative browser completion `1183.537`, `1669.773`, and `1290.756 ms`. The initial wrapper stopped before combo because no eligible existing fixture was available; it did not repeat payment or replacement. An identity-bound staging-only item/combo fixture was created, reconciled, used for one exact race, and archived in combo-then-item order with stock 20 preserved, no cleanup stock movement, empty floor, and compatibility v718. Deployed read-only definition/plan evidence SHA-256 `43e8ffab19d95f812716914349890ff3bb63ee774d43ecee2f42d808beb84336` proves no financial-v2 `app_state` reference. Authenticated staging log evidence SHA-256 `fd973cc6243b613a5d5cb8a4edfcf51ac8a583f3d0ede52afb7f67a15f9cc7b7` found no SQLSTATE `57014`, `40P01`, deadlock, or timeout signature and classified the sole target-run `P0001` as the expected losing checkout.
- [x] Independent test-agent final report returned **STAGING RELEASE B GO**. It independently verified the exact mixed/deployed/log hashes, fixture cleanup SHA-256 `cdbd15aa163cc583500f0f8080f7fa699391cc1c711c1c57901d063fdd6b2de6`, the non-replayed stopped wrapper, the corrected SHA-bound 50-case evidence, and the previously closed functional/concurrency gates. No mandatory failed, blocked, or unexplained not-run staging case remains. Production remains NO-GO until explicit approval and production-specific preflight/change controls.
- [x] Production build passes; lint exits zero with five existing warnings and no new warnings.
- [x] Prepare the production release locally without production access. The ignored production environment targets exact project `rrdwbxvuwrbxefarxnse` with every Release B flag true; project-local Wrangler `4.127.1` is pinned; the production-only bundle `dist/assets/index-B23LCT31.js` has SHA-256 `77efff31803b6e12322b1208253e1d30c1b6b840cafbace741b8435724bc7343`; its verifier finds all three v2 RPC paths and no staging reference; and Wrangler dry run exits without deploying. The fail-closed preparation script, pure read-only production SQL probe, exact-version rollback runbook, and five new contract tests pass. Final local closeout is 54 test files / 532 tests, TypeScript, production build, lint 0 errors / 5 baseline warnings, and diff integrity. Production remains untouched.
- [ ] Obtain explicit production approval, run the production-specific read-only preflight in a no-active-session window, capture the stable Cloudflare version, reconcile any installed-SQL drift, deploy once, and verify the first controlled bill plus 30-minute/end-of-day logs.

The current independent static verdict and environment-blocked evidence are recorded in `independent-audit.md`.
