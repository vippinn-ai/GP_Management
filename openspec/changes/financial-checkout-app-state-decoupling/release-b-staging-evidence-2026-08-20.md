# Release B staging execution evidence — 2026-08-20

## Scope and current decision

- Environment: `test/staging` Supabase project `tkbdyzxwwbhkpztgjjxh` and Cloudflare Worker `gp-management-staging-pages`.
- Production project `rrdwbxvuwrbxefarxnse` was not queried, migrated, deployed, or otherwise changed during this Release B execution.
- Financial v2 is enabled only in the staging build. The currently deployed frontend is Worker version `26e87eb7-8700-41a5-b8b2-4a7afde2e5d4`, bundle `/assets/index-BFQ2w3Qb.js`, SHA-256 `1979dc2379b412b9d9b7159faebac4ff54d1041fd3c433c188dea56c7bdad5fb`, from source checkpoint `6824cbe`.
- The installed Phase 10 SQL SHA-256 is `68ca3612d4e4bb3e5bfa5eca3607431552b0248a436e43dc8d99eb1e7db3b426`.
- The installed hardened Phase 6 admin RPC SHA-256 is `1bf2b24d337a913009bb75f8d2dc9ca4636a42073492b12e0be696f200db4c47`.
- The installed Phase 4 hop RPC SHA-256 is `f9dcd488cba735aeecbb6eb47da00626eb2fe2815811049cc8f9768e8527aef4`.
- Current decision: **Release B production NO-GO**. The evidence below closes the basic transaction, security and role boundaries, idempotency, duplicate-money, lifecycle, exact- and over-capacity limited-stock safety, both admin-metadata race orderings, both checkout/reject orderings, checkout versus standalone settlement, replacement, refund, live repeat-session-combo mutation, direct live session-item mutation, all twelve customer-tab item/combo mutation orderings, all authoritative-admin and ordinary receptionist/manager checkout-hop direction/order combinations, single- and multi-hop double-billing prevention, repeated-session performance, realtime, and reload subgates. Void/write-off contention, representative complex/mixed performance, remaining payment/discount/LTP paths, final query-plan/error capture, and independent final sign-off remain open.

## Installed definition and security evidence

The final Phase 10 migration was applied atomically in the staging SQL editor and returned `Success. No rows returned`. Read-only definition verification proved both `commit_checkout_bill_v2` and `commit_financial_adjustment_v2` contain the core metric, compute the final `server_duration_ms` after `financial_mutations.status = 'committed'`, and retain authenticated execute grants.

The final staging database smoke completed at `2026-08-20T14:45:57.964Z`, with zero retries and production access disabled:

- anonymous checkout rejected with `42501`;
- wrong organization rejected with `organization_access_denied`;
- client actor spoof rejected with `actor_spoof_rejected`;
- malformed checkout rejected with `invalid_payload`;
- absent mutation lookup returned `null`;
- authenticated actor and organization resolution passed.

Both ignored browser credential slots currently resolve to the same staging admin identity, so the command-line smoke explicitly reports its two-identity role case as `not-run`; it does not claim a false independent user. The retained, rollback-safe [`release-b-role-authorization-proof.sql`](./release-b-role-authorization-proof.sql) executed in the signed-in staging editor; the hardened retained copy was re-executed at `2026-08-20 15:21:58.452954+00`. Inside one transaction it changed that profile and its triggered organization membership to `receptionist`, established the authenticated JWT actor, and called the real v2 RPCs. Write-off, void, refund, and bill replacement each returned `role_access_denied`; settlement passed the role boundary and reached domain validation. The transaction rolled back, reset the SQL session role, and returned `passed | admin | admin`, proving that both profile and membership roles were restored.

The retained, rollback-safe [`release-b-inactive-authorization-proof.sql`](./release-b-inactive-authorization-proof.sql) then executed at `2026-08-20 15:48:29.8673+00`. Inside one transaction it set the same staging profile inactive and verified the profile trigger also made the organization membership inactive. With that user's authenticated JWT context, both `commit_checkout_bill_v2` and `commit_financial_adjustment_v2` rejected before domain validation with `organization_access_denied`. The transaction rolled back, reset the SQL session role, and returned `passed | true | true | admin | admin`, proving both active flags and roles were restored. No financial row was created because both calls were rejected at the organization-access boundary.

The hardened Phase 6 admin RPC was then installed additively in staging and returned `Success. No rows returned`. It derives the actor from `auth.uid()`, rejects a mismatched top-level actor, restricts inventory/category/combo/stock mutations to the server-resolved admin role, locks affected normalized inventory rows in deterministic ID order, and rejects missing, malformed, or stale `expectedStockQty` before writing. The concurrency-only precondition is stripped before normalized `raw_data` and compatibility `app_state.data` persistence.

The rollback-only [`release-b-admin-inventory-precondition-proof.sql`](./release-b-admin-inventory-precondition-proof.sql) passed at `2026-08-20 16:09:27.325935+00`. Missing, JSON null, string, object, array, stale-number, and precondition-bearing missing-row cases all returned `inventory_conflict`; the transaction rolled back with the Vipin profile active/admin. The rollback-only [`release-b-admin-data-authorization-proof.sql`](./release-b-admin-data-authorization-proof.sql) passed at `2026-08-20 16:20:01.646769+00`: receptionist and manager inventory attempts returned `role_access_denied`, while the same manager successfully wrote an expense, customer, audit, operational event, and compatibility delta inside the transaction. The audit and event used the authenticated actor. Rollback restored profile and membership to active/admin; a separate query at `2026-08-20 16:20:25.782312+00` found no fixture expense, customer, audit, event, or `app_state` content.

## Functional and two-browser evidence

All listed Playwright runs used the exact staging host/project guard, two isolated Chrome contexts where applicable, zero retries, disabled traces, ignored local credentials, compact JSON RPC evidence, and failure-only screenshot/video capture.

| Run | Case | Result |
| --- | --- | --- |
| `20260820131857` | Timed session checkout with edited end time | Passed after exact client/server audit-transition alignment; one canonical bill/event and no session resurrection. |
| `20260820132447` | Customer-tab inventory checkout with standalone variant, cigarette pack, and consumables combo | Passed; exact Maggie `-2`, cigarette `-10`, and Thumsuyp `-1` deltas, observer closure, and hard-refresh parity. |
| `20260820135005` | Invalid arithmetic rollback plus two simultaneous copies of one mutation | Passed; invalid mutation left no status row, concurrent replay returned one canonical bill/event. |
| `20260820134814` | Deferred checkout followed by v2 settlement | Passed; pending-to-issued transition, one settlement payment, observer parity, and hard refresh. |
| `20260820141104` | Two different mutations against the same session | Passed; exactly one mutation committed, loser rolled back with no status row, winner replay returned the original canonical result. |
| `20260820142216` | Two different settlement mutations against one pending bill | Passed; exactly one settlement committed and the loser left no mutation record, preventing over-collection. |
| `20260827053236` | Checkout versus standalone settlement race | Harness-only failure before either race command was intercepted or sent: the open session modal blocked a redundant dashboard click. One Rs 45 deferred setup bill remains pending with zero payments; the exact second session was rejected through guarded cleanup, and the staging floor is empty. No concurrency claim is made. |
| `20260827115031` | Checkout versus standalone settlement race | Functional/database Passed; Playwright remains reported Failed on a later cleanup-eligibility assertion (`open` versus normalized `active`). Standalone settlement committed one exact payment/audit/event, checkout returned the asserted `settlement_conflict` with null mutation and no candidate bill, compatibility stayed unchanged across the race, and separate exact-session rejection left an empty floor. |
| `20260820142733` | Two valid sessions concurrently claiming one bill number | Passed; exactly one `BILL` committed, loser received the explicit concurrent duplicate-financial-row error, loser mutation remained absent, and the still-open losing session was rejected through guarded cleanup. |
| `20260820143100` | Paid inventory bill refund | Passed; canonical Refunded status, one stock reversal, one audit, no fabricated payment row, observer realtime, and hard-refresh parity. |
| `20260820143246` | Unchanged bill replacement | Passed; original and replacement bills linked, original Replaced, replacement Issued, zero replacement stock movement, observer realtime, and hard-refresh parity. |
| `20260820150530` | Two simultaneous limited-stock customer-tab checkouts | Passed; a dedicated stock-2 item had two one-unit open reservations, both independent v2 submissions returned HTTP 200, both tabs closed, both browsers reloaded at stock zero with no open reservation, and the item was archived. |
| `20260820151921` | Two distinct checkout mutations against one hopped session | Passed; one request issued `BILL-20260820-134`, the competing request returned `session_not_billable`, the loser mutation remained absent, and replay of the winner returned the same bill and event. |
| `20260820153726` | Two stale limited-stock commands with demand above physical stock | Passed; both requests returned `inventory_conflict`, neither mutation/event/bill existed, physical stock remained `1`, and both source tabs remained open until guarded cleanup. |
| `20260820161556` | Checkout concurrent with an admin metadata edit carrying observed stock `2` | Passed with zero retries; admin committed first, checkout applied `-1` afterward, both browsers showed final stock `1`, and the item was archived. |
| `20260820162351` | Admin inventory create/edit/restock/deduct/archive/restore lifecycle | Passed with zero retries; all seven purpose writes were distinct, stock reconciled `3 + 2 - 1 = 4`, and final cleanup archived the item. |
| `20260824084853` | Checkout-first stale admin metadata command | Passed with zero retries; checkout committed once, the stale admin command returned `inventory_conflict`, final normalized stock was `1`, and no admin event or leaked precondition remained. |
| `20260824092648` | Rejection commits before a stale checkout | Passed with zero retries; rejection closed the session once, checkout returned `session_not_billable`, and no bill, payment, financial mutation, checkout event, or checkout audit existed. |
| `20260824104834` | Checkout commits before a stale game hop | Passed with zero retries; checkout issued one Rs 30 bill/payment, hop returned `session_not_open`, the losing hop produced no event/audit, and `app_state` remained version `613`. |
| `20260824104834` | Game hop commits before checkout | Financial/database result reconciled; hop and checkout each returned 200, one Rs 30 bill/payment existed, and the session ended closed/billed. The reported Playwright result remains failed because the then-deployed older bundle retained stale `Continue Customer`; the runner did not retry it. |
| `20260824105522` | Checkout and game hop submitted simultaneously after staging fix deployment | Passed with zero retries; both commands committed, one Rs 30 bill/payment existed, the session ended closed/billed, compatibility advanced once for hop, and the observer closed stale `Continue Customer` before reload. |
| `20260824180400` | Exact capture-only recovery of the rejected three-session chain | Passed with zero retries; both carried start/end tuples matched typed database values, primary start matched, no checkout was sent, and compatibility remained unchanged. |
| `20260824180700` | Guarded single-send cleanup of that exact chain | Passed with zero retries; one Rs 170 bill/payment and three linked session-charge lines closed all three sessions, with exact actor attribution and unchanged compatibility state. |

Earlier failed runs are retained as harness evidence and are not relabeled: `20260820135449` exposed the stale display-clock future-time boundary; `20260820142409` and `20260820142540` attempted timed-session controls on unit-sale Arcade sessions; `20260820142646` proved the bill collision but failed cleanup because two dialog handlers raced. Each harness defect was corrected, characterized, and followed by the passing run above without retrying an ambiguous financial mutation.

Limited-stock setup attempts `20260820145030` and `20260820145330` (before the final pass) are also retained. They stopped before any financial request: the first exposed a Category locator issue, and the second exposed premature `Synced` sampling plus tab-selection/realtime timing. Exact RPC waits and direct tab-chip activation corrected the harness. The aborted `20260820145330` tabs were closed through `reject_customer_tab`, and its dedicated item was archived through the normal admin UI. No abandoned QA tab or active item remains from those attempts.

Admin-race run `20260820161245` completed its financial and cleanup behavior correctly but failed a harness-only assertion because the admin browser did not emit an abort-induced `TypeError: Failed to fetch`. Read-only reconciliation before any new financial run proved admin metadata version `594` committed before the v2 checkout, the checkout created exactly one bill/payment/`-1` sale movement, final normalized stock was `1`, every persisted actor was Vipin, the fixture was archived at version `595`, and neither normalized `raw_data` nor `app_state` contained `expectedStockQty`. The harness now accepts zero or one exact abort error per browser and rejects any other page error.

Fresh zero-retry run `20260820161556` then passed. It captured the checkout and admin commands exactly once each; both returned HTTP 200, with mutation `financial-09b46114-2d6f-4caf-8af7-13453ea2beca`, bill `bill-ac6aca88-f1c1-4eff-87d6-d5544db353a5`, checkout event `event-a97a8424-df06-42f0-9668-597dad05a6ed`, and admin event `event-6ab18113-b69f-419c-acb3-47bf92412edd`. Database reconciliation at `2026-08-20 16:16:57.060827+00` proved final stock `1`, price `2`, one Rs 1 bill/payment, one `-1` sale movement, a committed mutation, matching Vipin bill/payment/movement/event/mutation actors, no persisted precondition, and archived fixture state. Compatibility versions `596`, `597`, and `598` correspond exactly to fixture creation, concurrent admin metadata commit, and archive; the normalized-only v2 checkout did not create an additional compatibility version. This proves the admin-first ordering.

Fresh zero-retry run `20260820162351` exercised the complete isolated admin inventory lifecycle through the deployed UI: new item at stock `3`, metadata price edit `1 -> 2` with stock unchanged, restock `+2`, direct adjustment `-1`, archive, restore, and final cleanup archive. It produced seven distinct events and consecutive `app_state` versions `599` through `605`, with no browser error. Read-only reconciliation at `2026-08-20 16:24:48.70098+00` proved normalized and compatibility snapshots both ended at price `2`, stock `4`, inactive/archived, and the same Vipin archive actor. The exact normalized movements were `restock +2` and `adjustment -1`; the seven audits were create, update, two stock movements, archive, restore, archive; all audit, movement, event, and compatibility actors were Vipin. Neither normalized `raw_data` nor the full compatibility document contained `expectedStockQty`.

On 2026-08-24 the same Playwright race became data-driven with reusable concurrent and deterministic checkout-first variants; the historical run above retains the observed admin-first evidence. Zero-retry run `20260824084358` first proved the checkout-first behavior through the deployed UI and RPC responses. Enhanced run `20260824084644` repeated the correct checkout/rejection/cleanup behavior but is retained as a harness-only failure because a new direct read of protected `financial_mutations` correctly returned HTTP 403 after the commit. The harness then switched to the authenticated `get_financial_mutation_result` RPC rather than weakening table security.

Fresh zero-retry run `20260824084853` is the canonical checkout-first evidence. The command was captured once and committed as mutation `financial-3bb5d5f9-cb6b-4333-92e2-b54c89f9a107`, bill `bill-dd9e43e6-ff1c-4cd5-a211-a263ceb0a23a`, and event `event-4a280920-e4b0-4b3f-bf1b-c3007383f37a`. The stale admin command carried observed stock `2`, returned HTTP 400 `inventory_conflict`, and produced no event. Embedded authenticated reconciliation proved exactly one bill, payment, `-1` sale movement, matching mutation-status result, and checkout event; all financial/event actor IDs resolved to Vipin. Normalized stock ended at `1`, price remained `1`, both normalized and compatibility JSON lacked `expectedStockQty`, both browsers agreed, and guarded cleanup archived the fixture. This closes the checkout-first stale-admin ordering without weakening `financial_mutations` RLS.

The reusable checkout/reject race was independently preflighted before any staging mutation. Run `20260824092142` is retained as a pre-race harness-only failure: one exact QA session started, the observer missed its realtime event, no checkout or race-rejection command was captured or sent, and one guarded `reject_session` cleanup closed session `session-8e787215-cbee-43c4-90ba-72437a906ff1`. Run `20260824092340` then completed and reconciled the checkout-first ordering before failing only its post-reconciliation UI expectation: mutation `financial-bcf33349-3470-40e0-ad4e-2f1ddcf1bfbe` issued Rs 30 bill `bill-c0079a07-8134-4cb9-84fb-79c79fa21256`; the stale rejection returned `session_not_open`; exactly one bill, payment, checkout event, and two checkout audits existed; all actors matched authenticated Vipin; the session was closed/billed; capture counts remained `1/1`; and `app_state` stayed at version `612`. Both browsers showed the station Available. The loser browser correctly retained one local conflict, so the reusable harness now asserts that explicit conflict, proves no pending sync, clears it locally without retrying, and then requires `Synced`. The independent reviewer accepted this canonical database evidence and prohibited an unnecessary checkout-first rerun.

Fresh zero-retry run `20260824092648` executed only the previously unrun reject-first case. Rejection mutation `op-c995f839-a87a-4fef-87eb-1e4b2b362a9f`, event `event-d4bdaf8e-01ae-4a3a-8622-90c4897b6e84`, and audit `audit-aa5b1a63-4297-4eb4-ad09-80bf31bb6383` closed session `session-34d8fa30-c4b2-4744-859c-16aa2194bcba` as rejected with the exact reason. The stale checkout mutation `financial-0d5e2fed-6ab6-49de-ae0b-77881ae7911f` returned `session_not_billable`; its status lookup was `null`. Reconciliation proved zero bill, payment, financial mutation, checkout event, or checkout audit, one rejection event/audit, authenticated Vipin attribution, exact final session state, command counts `1/1`, and the expected single compatibility change from version `612` to `613`. Both browsers reloaded with the station Available. This closes checkout concurrency against rejection in both deterministic orderings.

The reusable checkout/hop suite was independently reviewed twice before staging. Its shared interceptor allows one UI capture, one explicit `route.fetch`, blocks duplicate captures, and prohibits automatic retry or post-send cleanup while a result is ambiguous. Before `raceStarted`, each browser independently calls the same authoritative `current_user_org_role` function used by Phase 10 and requires active organization role `admin`; the profile role is recorded separately. The suite is therefore deliberately scoped to admin-versus-admin timing edits and does not claim receptionist/manager success semantics.

Run `20260824104834` used the older deployed bundle `/assets/index-C9CwZnTb.js`. Checkout-first passed: mutation `financial-fc46cf4a-860e-4fe6-8f12-ff2267bfdc9b` issued bill `bill-f90ee05c-0bed-46b3-8e13-fb838f2b1cf0` / `BILL-QA-HOP-20260824104834-A`, one Rs 30 payment, event `event-51de255a-4102-4114-8475-dfc33752ae66`, and two checkout audits. The stale hop returned HTTP 400 `session_not_open`, produced no hop event/audit, command counts stayed `1/1`, the exact losing-hop alert and one local conflict were shown, the session ended closed/billed, and `app_state` stayed at version `613` because v2 did not touch it.

The same zero-retry run then reconciled hop-first completely before its UI-only failure. Hop mutation `op-c1debb49-7866-4269-b3b1-ca8e56a52e6b` committed event `event-6dc36bb3-f172-4611-87f6-e220242ecb54`; checkout mutation `financial-84be3e09-b787-4e82-9e8f-caf986837905` issued bill `bill-cace9ac9-9b4e-4f3c-82b3-7365fc796dc9` / `BILL-QA-HOP-20260824104834-B`, one Rs 30 payment, one checkout event, two checkout audits, and one hop audit. All persisted actors matched authenticated Vipin. The session ended closed/billed, mutation lookup returned the same bill, and compatibility advanced exactly once from `613` to `614` for the hop. The old frontend rejected any normalized update to an already closed local session, so it discarded the canonical `closed/hopped -> closed/billed` overlay and left `Continue Customer` visible. The test stopped, retained the failure, and skipped the concurrent title; no financial command was retried.

Checkpoint `2483c7b` fixed that product defect narrowly: normalized merge accepts a canonical closed-to-closed transition but still rejects closed-to-nonclosed resurrection, and the app clears a post-hop continuation once its source is billed, consumed, or unavailable. Unit tests cover both merge directions and continuation recoverability. Local gates passed before deployment. Wrangler `4.125.0` dry-run validated 14 assets with no bindings; staging-only deployment uploaded three changed files and produced Worker version `f76e2641-5c10-4564-a596-b283ddfbd415`. Production was not accessed.

Only the previously skipped concurrent title then ran as zero-retry run `20260824105522` against bundle `/assets/index-BQ1NTp6g.js` (SHA-256 `61bb9a76898e9dbafd1c2b563a68637b496c9ad711cbd0b4d4ae9d3d022c7314`). Both commands committed: hop mutation `op-75a4c94a-288d-4213-b53d-116b1e140b52` and checkout mutation `financial-2cefdf1c-bcd9-4b74-9885-20f26f2d4d23`. Exactly one Rs 30 bill `bill-c6c855a6-ae43-497d-a35d-cf070eacf299` / `BILL-QA-HOP-20260824105522-C`, one payment, one checkout event, one hop event, two checkout audits, and one hop audit existed. All financial/event actors matched the authenticated admin. The session ended closed/billed with that bill, `app_state` advanced once from `614` to `615` for hop only, capture counts stayed `1/1`, both browsers had zero console/page errors, and `staleContinuationClosedBeforeReload` was true. This closes the three admin checkout/hop orderings without relabeling or rerunning the retained hop-first UI failure.

The passing exact-capacity artifact contains two distinct bill IDs, mutation IDs, event IDs, and stock-movement IDs. Read-only database reconciliation proved two bills totaling Rs 2, two payments totaling Rs 2, one bill actor and one payment actor, two committed mutations, two v2 checkout events, maximum recorded database duration `318.560 ms`, two movements totaling `-2`, two closed tabs, final normalized stock `0`, open reservations `0`, and archived item state. The two `TypeError: Failed to fetch` page errors are expected from deliberately aborting the UI submissions after capturing each command; the two captured commands were then sent exactly once through independent API contexts.

The over-capacity run created an isolated stock-2 item, reserved one unit in each of two open tabs, captured both valid commands, and then recorded one audited admin deduction so physical stock was `1` while combined reservations remained `2`. Both independent requests returned HTTP 400 with `inventory_conflict`; both mutation-status lookups were `null`. The two source tabs were still visible after hard reload and physical stock displayed zero available with one unit reserved. Database reconciliation after guarded cleanup proved physical `stock_qty = 1`, exactly one `-1` admin adjustment, zero checkout bills, zero financial mutations, zero checkout events, both tabs rejected with no bill, zero remaining open reservations, and the dedicated item archived. Harness-only runs `20260820152459`, `20260820152759`, `20260820153046`, `20260820153209`, `20260820153423`, and `20260820153552` exposed dialog, available-versus-physical stock, and post-reload navigation assumptions; they created no committed checkout, and their exact QA tabs/items were closed or archived before the next run.

The first hopped-race run `20260820151441` stopped before any financial request because its recovery flow detached the continuation and then targeted a stale modal. Its exact QA hopped session was billed once through the staging UI as unambiguous cleanup; it was not retried as a test result. The corrected zero-retry run `20260820151921` billed directly from the post-hop continuation and raced two distinct mutation IDs. Responses were HTTP `[400, 200]`: the loser returned `session_not_billable`, its mutation-status lookup was `null`, and replaying the winner returned the original `bill-03d08bdf-0ee1-4863-abc9-35e85507df79` and `event-2348b701-7b8c-4a86-831d-76a5bf8b8267`. Read-only database reconciliation proved exactly one bill and one payment for the source session, one committed mutation (the winner only), one operational event, actor equality, session status `closed`, close disposition `billed`, and the same closed bill ID. This closes the one-hop/two-bill race; multi-hop chains, receptionist/manager checkout-hop timing outcomes, and concurrency against other writers remain open.

## Multi-hop timing diagnostic and guarded correction — 2026-08-24

Zero-retry multi-hop run `20260824172400` successfully created a real three-session chain but both distinct checkout commands returned HTTP 400. Read-only reconciliation proved both mutation lookups were null, both candidate bills/payments/events were absent, the exact three-session chain remained unbilled, and `app_state` stayed at version `620` with SHA-256 `21af0a2d1f08c80ed0d98cd7442d4cba95ff04b62871c8f6bbe38efae4049bc0`. A guarded single-command attempt `20260824174100` then returned deterministic `invalid_session_timing`; its exact mutation lookup was null and it produced no financial rows or compatibility change. Neither failed mutation was retried.

Capture-only run `20260824174500` sent no checkout. Its retained request/database comparison isolated the defect: the oldest carried session submitted stale compatibility `startedAt = 2026-08-24T11:53:15.991Z`, while locked normalized `sessions.started_at = 2026-08-24T11:41:00+00:00` and `ended_at = 2026-08-24T11:44:00+00:00`. The second carried session matched exactly. The server therefore correctly rejected an impossible start-after-end interval. The run's reported Playwright failure was harness-only: capture cancellation and immediate route removal both attempted to handle the same intercepted request. No checkout was sent.

The correction is deliberately narrow. The normalized mapper now prefers only typed session start/end timing over compatibility JSON; other mapping precedence is unchanged. Typed null end suppresses stale raw data, while the legacy null-start fallback remains characterized. The hop RPC locks normalized `started_at`, rejects a missing value, and copies the locked timestamp into compatibility JSON before persisting the hop. The reusable interceptor exposes a first-request completion promise, and capture-only cleanup awaits it before removing the route. Independent review issued GO for this minimal fix, staging deployment, and one exact capture-only rerun. Before deployment, 39 Vitest files / 429 tests passed, the production build passed, lint remained at zero errors/five existing warnings, and v2 Playwright discovery listed 24 zero-retry cases. Production remained untouched and remains NO-GO.

Checkpoint `ad9fa5b` was pushed before deployment. Wrangler `4.125.0` dry-run read 14 staging assets with no bindings; staging-only deployment uploaded three changed assets and produced Worker version `8d3fd008-db2f-4ef1-98c1-a467f6e55db5`. The reviewed hop definition was then installed only in project `tkbdyzxwwbhkpztgjjxh`. A read-only definition/grant query returned `true | true | true | true | false` for locked typed start, null-start rejection, raw-start canonicalization, authenticated execute, and anonymous execute. Production was not accessed.

Capture-only run `20260824180200` stopped before capture or send because a neighboring Available card intercepted the narrow edge of the real Running card's Manage button. The screenshot proved the session and customer were displayed correctly; this disproved a stale-status hypothesis. The reusable helper now asserts that the native Manage button is visible/enabled and activates it through keyboard focus/Enter, retaining the exact dialog and stored-customer guard without forced or DOM clicks. Independent review approved this harness-only correction.

Fresh capture-only run `20260824180400` passed with zero retries against the exact deployed bundle. `checkoutSent` remained false, both carried session start/end instants matched typed normalized rows, the primary start matched, `app_state` remained version `620` with SHA-256 `21af0a2d1f08c80ed0d98cd7442d4cba95ff04b62871c8f6bbe38efae4049bc0`, and no dialog, console, or page errors occurred. After independent GO, one fresh single-send run `20260824180700` committed mutation `financial-active-multihop-20260824180700`, bill `bill-f70b28eb-c41c-4e53-88e8-a55d7d11e71f` / `BILL-QA-ACTIVE-MULTIHOP-20260824180700`, payment `payment-512854a4-bee8-49af-96fb-d2866fee081b`, and event `event-d8a4805a-02b3-43ff-8f47-52344d3a42fc`. The canonical Rs 170 cash bill contains exactly three session-charge lines (Rs 10, Rs 10, and Rs 150), all three sessions are closed/billed to it, all financial/event/audit actors match Vipin, database duration was `229.914 ms`, and the compatibility version/hash remained unchanged. This closes the abandoned chain without retrying either rejected mutation.

Subsequent fresh multi-hop runs retained zero-retry/single-send behavior but exposed two distinct realtime/rebase races before financial submission. Runs `20260824181100` and `20260824182700` showed the continuation modal being cleared by incomplete or transient normalized source state. Guarded cleanup runs `20260824182300` and `20260824182900` each issued exactly one Rs 10 bill with one line/payment/event/audit, authenticated actor attribution, sub-200 ms server time, and unchanged compatibility data hash. Checkpoints `ec2b973` and `6824cbe` narrowed continuation clearing to positive terminal evidence; the latter is the currently deployed staging frontend.

Run `20260824183400` then proved the continuation modal race closed: S1 `session-9ef998d5-e5a9-44f9-b68a-f815df023b7a` hopped and S2 `session-4f9e640d-877d-4817-a005-bc08e6d3a76c` started on the same station. S2 detail save was blocked locally after a delayed closed-S1 patch evicted the active S2 snapshot, so no checkout command was sent. The harness rejected S2 for cleanup, leaving S1 closed/hopped/unbilled and S2 closed/rejected/unbilled with the stale S1 link. Read-only artifact `test-artifacts/reconciliation/staging-sessions-20260824183400.json` records no bill line for either ID and compatibility version `624`, SHA-256 `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`.

The independently reviewed local correction now prevents a closed ID-scoped patch from evicting a different active same-station continuation and atomically releases continuation links when a session or customer tab is rejected. Every backend rejection is acknowledgement-required and non-optimistic; ambiguous failures retry the same mutation ID. Reject SQL derives the authenticated actor, validates operation identity after serialization, canonicalizes typed/raw/compatibility links and `closedBillId`, constructs release audit text from locked data, and rolls back on an audit-ID collision. This correction has passed local tests/build and independent product-code review but is not yet installed or deployed to staging. The existing S1/S2 chain remains quarantined pending the exact transactional SQL proof and purpose-built guarded repair.

## Corrected performance evidence

The first 50-case run `20260820140339` functionally passed but its SQL duration stopped before canonical hydration and the mutation completion update. That run is retained as functional load evidence only. Phase 10 now records `core_duration_ms` separately and computes `server_duration_ms` after all domain writes, event insertion, canonical hydration, and mutation commit; only persistence of the timing metric itself follows.

Corrected run `20260820141559` executed 50 sequential Arcade 1 unit-sale session/inventory checkouts in one zero-retry test:

- 50/50 checkout RPCs returned HTTP 200;
- 50 unique session, bill, bill-number, event, and mutation IDs;
- bills `BILL-20260820-075` through `BILL-20260820-124`;
- full-domain database p95 `478.066 ms`, maximum `530.476 ms`;
- browser p95 `4,261 ms`, maximum `5,051 ms`;
- database and browser thresholds passed (`<2 s` p95, `<5 s` DB max, `<7 s` browser max);
- cleanup recorded 50 completed, no active QA customer, no cleanup error, no dialogs, and no remote errors.

Read-only database reconciliation matched the artifact exactly: 50 issued bills totaling Rs 250, 50 payments totaling Rs 250, 50 operational events, 50 mutation IDs, 100 referenced audits, and 50 stock movements totaling `-50`. Bill, payment, and audit actor sets each contained one authenticated actor.

Immediately before and after this corrected run, the legacy snapshot remained exactly:

- `app_state.id = 'primary'`;
- version `543`;
- SHA-256 `d75a28837a0640266056f80e835e03d7ae6d4012b901b498afff046904574023`.

This performance claim is deliberately scoped to repeated Arcade session/inventory checkout. It does not represent a mixed session/customer-tab/carryover/combo/settlement workload.

## Local gates

After the final source and SQL changes:

- Vitest: 39 files / 446 tests passed;
- production build: passed (existing large-chunk warning remains);
- lint: zero errors and five documented warnings;
- `git diff --check`: passed, with expected line-ending notices only;
- focused hop-reconciliation and staging-harness checks: 141/141 passed; the harness contract alone is 10/10;
- financial-v2 Playwright discovery: 24 tests across 14 files with zero configured retries; the Release A discovery runner correctly fails closed while staging has financial v2 enabled;
- current review manifest: 336 first-party files / 92,668 physical lines / 271 semantic hotspots / 252 billing-relevant files / 129 classified `app_state` references, with zero unclassified references.

## Remaining mandatory Release B gates

1. Complete the four remaining customer-tab concurrency cases (`remove_customer_tab_item / simultaneous` and all three `apply_customer_tab_combo` orderings), pending bad-debt write-off, and issued-bill void. Eight customer-tab add/update/remove orderings now pass by immutable reconciliation. Checkout versus direct live session-item mutation, standalone settlement, replacement, refund, live repeat-session-combo mutation, all six ordinary-role direction/order combinations, all three authoritative-admin checkout/hop orderings, both checkout/reject and admin-metadata orderings, single- and multi-hop double-billing contention, plus standalone metadata/direct-stock/restock/archive/restore/new-item actions already pass. The exact gap map and required harness contracts are in `remaining-release-b-gap-audit-2026-08-28.md`.
2. Live timed/unit/tab coverage completes UPI, split, partial/deferred collection during checkout, discount/LTP/rounding/zero-total boundaries, pauses, direct/multi-hop carryover, and changed replacement quantities.
3. A mixed representative performance/contention run passes and final deployed query plans/logs prove no `app_state.data` read, expansion, lock, patch, or rewrite and no SQLSTATE `57014`/deadlock.
4. Independent tester completes the final evidence review and issues a production recommendation. Production deployment still requires separate explicit approval and a no-active-session window.

## Reject-release transactional proof incident and reconciliation — 2026-08-24

The first exact-definition reject-release proof was independently approved for one staging SQL Editor execution only after a reusable read-only preflight reported zero non-closed sessions, zero open customer tabs, authenticated admin role, and baseline compatibility version `624` / SHA-256 `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`. The executed generated artifact had SHA-256 `d399c3ba7fc9b0f6673e19f46a445d7e3b6af4a85ecfd017be0e7dcaa706e1e9` and embedded exact local definitions for `start_session`, both reject RPCs, and `link_customer_tab_continuation`.

The run is retained as **failed** and was not retried. Supabase SQL Editor wrapped the multi-statement request in an outer transaction. The proof's explicit `ROLLBACK` therefore restored not only the exact definitions and fixtures created after its textual `BEGIN`, but also the two snapshot temp tables created earlier in the same request. The first post-rollback assertion failed with SQLSTATE `42P01`, `relation "qa_reject_rpc_definition_before" does not exist`. This is a proof-harness transaction-boundary defect, not a product-RPC assertion result; no `proof_result=passed` claim is made.

Two independent read-only reconciliations immediately followed. The empty-floor preflight still reported zero open sessions/tabs and exact version/hash `624` / `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`. Purpose-built artifact `test-artifacts/reconciliation/reject-rpc-proof-failed-run-20260824-1941.json` then checked every fixed session, customer-tab, station, audit, entity, operational-event mutation, and compatibility fixture ID. It found zero residual rows/IDs and the same exact compatibility version/hash. This confirms the attempted proof left no domain or compatibility fixture behind. Function definitions were not separately queried after the failure because the one-run/no-retry condition was honored; their restoration follows from the same successful outer rollback that removed the pre-transaction temp tables. Production was not accessed.

The replacement generator now uses the SQL Editor's outer transaction deliberately: create temp snapshots, establish `SAVEPOINT qa_reject_rpc_proof`, install exact definitions and execute all proof cases, `ROLLBACK TO SAVEPOINT`, release it, assert definitions/grants, exact `app_state`, and zero residual fixtures, drop the temp snapshots, and return one final `proof_result=passed` row with verified in-savepoint version delta `3`. Candidate artifact SHA-256 `42d9545e4ab1f11a05d7b4249409731a1df5a024cb9fb0e219e1951ba21e63b4` contains exactly one savepoint, one rollback-to, one release, and zero top-level `BEGIN`, full `ROLLBACK`, or `COMMIT`; focused local contracts pass `20/20`.

## Savepoint proof SQL Editor submission — 2026-08-24

After fresh user authorization and independent static approval, reusable preflight `savepoint-proof-20260824-1958` passed at `2026-08-24T17:50:56.814Z`: staging project `tkbdyzxwwbhkpztgjjxh`, organization `org-primary`, authenticated admin actor, zero open sessions, zero open customer tabs, and compatibility baseline version `624` / SHA-256 `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`. The exact candidate artifact SHA-256 was reverified immediately before submission.

The SQL Editor Run control was activated exactly once at `2026-08-24T17:51:30.566Z`. The interface showed `Running...`, switched to an empty `New` editor, and after approximately 30 seconds returned the client/API validation message `Error: query: Too small: expected string to have >=1 characters`. It returned neither a PostgreSQL SQLSTATE nor the artifact's final `proof_result` row. The attempt is therefore **inconclusive/not run at the database evidence level**, not passed. It was not retried.

Read-only reconciliation `savepoint-ui-error-20260824-2322d` passed at `2026-08-24T17:54:10.944Z`. It found exact compatibility version/hash `624` / `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`, zero residual sessions, tabs, stations, audit rows, entity events, mutation events, or fixture IDs in `app_state`. Final read-only preflight `post-savepoint-ui-error-20260824-2324` then passed at `2026-08-24T17:54:25.253Z` with zero open sessions/tabs and the same exact baseline. Staging is clean and production was not accessed, but the reject-release transactional proof remains an unresolved Release B gate.

## Verified SQL submission and explicit-transaction correction — 2026-08-24

A compact read-only SQL Editor probe was loaded by clipboard paste and returned four deployed definition/grant baselines at `2026-08-24T18:08:46.432Z`. `start_session(jsonb)`, `reject_session(jsonb)`, `reject_customer_tab(jsonb)`, and `link_customer_tab_continuation(jsonb)` all allowed `authenticated` execution and denied `anon` execution. Their respective deployed `pg_get_functiondef` SHA-256 values were `29de081cdb0c948c9c2d23d0cb2b8ce88caec6e3155db33fbcd2396a27ebe294`, `f71f06a469842d50b3282c94f40e2df748d4b950875447ee422470dc31584740`, `354fcf4ebc176daa21ab768032f0c85f5259a7dbbc82e8647363854818c9b3df`, and `1a79c30d0c64fdf9a4c30d2d94c80a30ec33207df5fa144a3e049fe0de546e48`.

Fresh reusable preflight `savepoint-final-20260824-2340` passed at `2026-08-24T18:09:18.490Z` with zero open sessions/tabs and exact compatibility version/hash `624` / `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`. Candidate SHA-256 `42d9545e4ab1f11a05d7b4249409731a1df5a024cb9fb0e219e1951ba21e63b4` was loaded through clipboard paste. Copying the full editor content back proved semantic equality after normalizing Monaco's line endings, and both the opening safety marker and final result assertion were visible before the one authorized submission.

The database returned SQLSTATE `25P01`, `SAVEPOINT can only be used in transaction blocks`, immediately at the savepoint. This proved that the current SQL Editor execution path does not supply the assumed outer transaction. No exact function definition, fixture, or product mutation statement after the savepoint was reached, no `proof_result` was returned, and no retry was issued. Read-only reconciliation `savepoint-25p01-20260824-2341` then proved exact compatibility version/hash and zero residual fixture rows. A second definition/grant probe at `2026-08-24T18:11:17.660Z` matched all four pre-run rows exactly. Final preflight `post-savepoint-25p01-20260824-2344` passed at `2026-08-24T18:14:48.559Z` with zero open sessions/tabs and the same exact baseline. Production was not accessed.

The corrected generator now emits one explicit outer `BEGIN`, creates snapshots, establishes one `SAVEPOINT`, runs exact definitions and proof cases, performs one `ROLLBACK TO SAVEPOINT` and one `RELEASE`, verifies definition/grant/app-state restoration and zero residual fixtures, drops the temporary evidence tables, issues one `COMMIT`, and only then returns the final proof row. It contains zero full `ROLLBACK` statements. A fail-closed generator guard verifies this exact ordering after normalizing line endings. Corrected candidate SHA-256 `db8816741351215d4f81e0fe53e99ccb434917d034322a0e6a5fd82c9d8f4662` passed the focused `20/20` contract suite and received independent static GO before the separately authorized execution documented below.

## Reject-release transactional proof passed — 2026-08-25

Following fresh authorization, the corrected candidate was regenerated from commit `6560bbf` and reverified at SHA-256 `db8816741351215d4f81e0fe53e99ccb434917d034322a0e6a5fd82c9d8f4662`. Fresh reusable preflight `explicit-tx-final-20260825-0001` passed at `2026-08-24T19:17:20.107Z` (`2026-08-25 00:47:20 IST`) with staging project `tkbdyzxwwbhkpztgjjxh`, organization `org-primary`, authenticated admin actor, zero open sessions, zero open customer tabs, and compatibility version/hash `624` / `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`.

The full `117,356`-character artifact was pasted into the authenticated staging SQL Editor. Copying the editor contents back and normalizing line endings proved semantic equality with the generated artifact; the opening explicit `BEGIN` and final proof assertions were visible. The Run control was activated once at `2026-08-24T19:18:20.627Z`, and the SQL Editor safety confirmation was accepted once at `2026-08-24T19:18:28.890Z`. No retry was issued.

The database returned one final result row with `proof_result = passed`, verified in-savepoint `app_state` version delta `3`, restored `app_state` version `624`, and restored PostgreSQL `data::text` SHA-256 `22368f8e74c3026017685fbc096f4281deb5c71277778c4b42691758ad743e51`. This final row was reachable only after the exact-definition proof body, rollback-to-savepoint, four-function definition/grant restoration checks, exact PostgreSQL app-state restoration check, zero-fixture assertion, temp-table cleanup, and outer commit completed successfully.

Reusable read-only reconciliation `explicit-tx-passed-20260825-0003` passed at `2026-08-24T19:19:10.670Z`. It independently confirmed the application-canonical compatibility baseline version/hash `624` / `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f` and zero residual sessions, customer tabs, stations, audit rows, entity events, mutation events, or fixture IDs in `app_state`. Final preflight `post-explicit-tx-passed-20260825-0004` passed at `2026-08-24T19:19:23.094Z` with zero open sessions/tabs and the same exact baseline. Independent final evidence review issued GO to close the reject-release transactional proof gate and confirmed that the differing Node/PostgreSQL hashes reflect their different JSON serializations. Production was not accessed. Overall Release B remains production NO-GO because receptionist/manager timing, multi-hop and remaining-writer contention, the complex payment/discount/LTP matrix, mixed performance, deployed query-plan/error evidence, and final production recommendation remain open.

## Reject-release persistent staging installation and correction deployment — 2026-08-25

The persistent staging installer was generated only from the three proven source files and had SHA-256 `ab6c41de0aa9d3adb7cf62f505e195ba983ce4b5d329ff3c3845b2ef026d1186`. It contained one explicit `BEGIN`/`COMMIT`, no rollback, and in-transaction definition, grant, and security-definer assertions for the four browser RPCs and two private helpers. Fresh preflight `reject-install-final-20260825-0108` passed with staging project `tkbdyzxwwbhkpztgjjxh`, zero open sessions/tabs, and exact compatibility baseline version/hash `624` / `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`.

The exact `69,109`-character installer was pasted and copied back with semantic equality after line-ending normalization, then submitted once at `2026-08-24T19:38:21.884Z`. The database returned six verified rows. Post-install definition SHA-256 values are: `link_customer_tab_continuation` `649d4fc5108e1b0e6b15c9a44e0a7cfd14af68ab506028c3ec39c1c8bd1a5f70`; `patch_app_state_array_by_id` `fc5cf3f1df9e2de04448f2c51454ff783e91789086af767d6619ae913b3e031d`; `raise_operational_rpc_error` `5cbbd7f9c1444ac8a628a4b7f76cba34cf022aa3bd9dfbbece9bcfc2726b2198`; `reject_customer_tab` `31e08cf35f4e9ef88494277400131c805a1d40bc58f504e7a6bd15a08c49c760`; `reject_session` `ca7323c0defd26f3a873623bee376c13d1f1a96dea1aa003ba6ebc9d317f3fbf`; and `start_session` `48c01f2e3adda16c8cb771e0d896014d84b251c398149bb987d4fe6cab1c16c6`. All four browser RPCs grant `authenticated`, deny `anon`, and are security-definer; both helpers deny both browser roles and are not security-definer. Read-only post-install preflight `reject-postinstall-20260825-0112` proved the floor and compatibility state were unchanged.

The correction candidate then passed 39 Vitest files / 448 tests, staging and production-mode builds, Wrangler staging dry-run, and lint with zero errors and the same five warnings. The staging Worker deployed successfully as version `f10e8360-96d3-480a-bf19-441594bbc175` at `https://gp-management-staging-pages.breakperfectgaminglounge.workers.dev`. Three changed assets were uploaded; production was not accessed.

## Quarantined S1/S2 repair failed closed and reconciled — 2026-08-25

Independent review issued GO for one staging-only repair attempt after the candidate added sorted session locks, compatibility-object preservation, normalized and compatibility audit-ID absence, designated-maintenance attribution, exact audit/event/app-state assertions, explicit timestamps, and a `FOR SHARE` membership lock. Immediately before execution, read-only evidence returned exact version/PostgreSQL hash `624` / `22368f8e74c3026017685fbc096f4281deb5c71277778c4b42691758ad743e51`, zero open sessions/tabs, one exact normalized and compatibility row for each quarantined session, one sole continuation reference, zero linked bill lines, one active designated admin, and zero repair-identity rows.

The exact executed repair artifact had SHA-256 `a20b6fceb6fa0f1b7674178e9877ccb72f41cb00c6df090b89407e7443f54e59`. Monaco copy-back matched the local file semantically, including its single `BEGIN`/`COMMIT`, exact baseline guard, and absence of the production project reference. The selected full script was submitted once at `2026-08-24T19:51:17.240Z`. It failed at SQLSTATE `QA040`, `Guarded repair audit or compatibility state verification failed`, and returned zero result rows. No retry was issued.

Two read-only reconciliations proved complete rollback. Reusable preflight `reject-postrepair-rollback-20260825-0122` returned zero open sessions/tabs and exact application compatibility version/hash `624` / `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`. The exact repair preflight was then rerun read-only and again returned PostgreSQL hash `22368f8e74c3026017685fbc096f4281deb5c71277778c4b42691758ad743e51`, both original continuation links, one sole reference, zero linked bill lines, and zero audit/event/compatibility repair identities.

Read-only trigger diagnosis identified the deterministic assertion mismatch. Deployed triggers `app_state_set_updated_at` and `sessions_set_updated_at` both execute `public.set_updated_at`, definition SHA-256 `00a8c647f5ea99964b50a2c0499bda024e3644d6fcb32dbe215f7b644de8b31b`; that function canonicalizes `updated_at` to transaction `now()`. The failed candidate instead asserted a later `clock_timestamp()`, so its own exact timestamp assertion forced the rollback. The unexecuted repair proposal now sets the transaction timezone to UTC, uses the canonical transaction timestamp, guards both enabled deployed trigger identities and function hash, and separates compatibility-state and audit failure codes. It is checked in with a `NOT_AUTHORIZED` sentinel that fails before locks or writes; a separately approved attempt must replace that sentinel and all three attempt identities. Its focused contract remains 22/22. A second staging attempt requires a new explicit user approval, fresh exact preflight, a new reviewed attempt identity, independent GO, one submission only, and post-run read-only reconciliation. Overall Release B and production remain NO-GO.

## Quarantined S1/S2 repair passed — separately authorized second attempt

The user explicitly approved one second staging-only repair attempt. Fresh reusable API preflight `qa-repair-a2-20260825-092610` passed at `2026-08-25T03:56:37.995Z` with project `tkbdyzxwwbhkpztgjjxh`, authenticated admin actor, zero open sessions/tabs, and exact application-canonical version/hash `624` / `985d47c04e857ee18266685558233d391545d73bd33242bf499e746c78a7970f`. A separate exact SQL preflight returned PostgreSQL hash `22368f8e74c3026017685fbc096f4281deb5c71277778c4b42691758ad743e51`, one exact normalized and compatibility row for each quarantined session, one sole source reference, zero linked bill lines, zero fresh repair-identity rows, and both enabled reviewed update triggers.

Fresh identities were generated only after approval: audit `audit-qa-continuation-repair-20260825-092610-a2`, event `event-qa-continuation-repair-20260825-092610-a2`, and mutation `maintenance-qa-continuation-repair-20260825-092610-a2`. The executable artifact was derived in memory from fail-closed template SHA-256 `d4e17ec7dfe53a1bf972494ad51ef511ceb3f92885add7a71a89bed64a31cd0e` using only the independently reviewed replacements: preparation comment once, audit ID twice, event ID twice, mutation ID once, and authorization sentinel once. Its raw SHA-256 was `5c5e4f9bf7b27ce165b59b7a5c440a6356c7b34a2b417d14ac4c04ca7543f5ae`; Monaco-normalized SHA-256 was `9ca2c28bb8cc1bb57e395804343141c5b143a74a0d825d17602cbfd8caf715be`. Copy-back proved semantic equality, old identities and `NOT_AUTHORIZED` were absent, the UTC/trigger guards remained, and the production project reference was absent. Independent review issued GO for exactly one submission.

The selected full artifact was submitted once at `2026-08-25T04:00:37.885Z` (`09:30:37 IST`). It returned one result row: `repair_result = passed`, compatibility version `625`, PostgreSQL data hash `dcc642b5abf78b54170fb14fb2b8802bb097d7902d380db5115face9ca8775c0`, and empty normalized/raw continuation arrays. No retry occurred.

Comprehensive read-only postflight at `2026-08-25T04:01:39.724Z` proved one exact compatibility consumer with an empty link, zero remaining references to S1, zero linked bill lines, exactly one normalized audit, exactly one compatibility audit, exactly one operational event, matching designated actor and `updated_by`, and equality of session/app-state/audit/event timestamps at `2026-08-25T04:00:38.42914Z`. It also proved version `625`, the same PostgreSQL hash, and zero open sessions/tabs. Reusable API postflight `qa-repair-a2-postflight-20260825-093210` independently passed at `2026-08-25T04:01:56.775Z` with application-canonical hash `de85de0a53d6fd62fd7d83bcaa2582cb8662d33a0121c0aba4e5ac830e413601` and the same empty floor.

Independent final review issued GO to close the staging quarantine-repair gate. The checked-in SQL remains fail-closed with `NOT_AUTHORIZED`; the in-memory authorized artifact was not written back. The next targeted product validation is the existing zero-retry Playwright case `one three-session hop chain can enter only one bill`, but it creates new staging sessions and a bill and therefore requires separate staging-write authorization. Production remains untouched and overall Release B remains NO-GO.

## Authorized three-session multi-hop run safely reconciled but product race remains inconclusive

The user separately authorized one staging-write execution of `one three-session hop chain can enter only one bill`. Fresh run ID `20260825102302` passed preflight at `2026-08-25T04:53:51.724Z` against staging project `tkbdyzxwwbhkpztgjjxh`, Worker bundle `/assets/index-7AYwZnhZ.js` with SHA-256 `30971d9e1b242d6d210d2bbf3732b9dad1d310da470e6e1558252745f47a71ea`, zero open sessions/tabs, compatibility version `625`, and application-canonical hash `de85de0a53d6fd62fd7d83bcaa2582cb8662d33a0121c0aba4e5ac830e413601`. The exact zero-retry command was `npm run test:e2e:staging:v2 -- tests/e2e/staging/release-b-multihop-concurrency-v2.e2e.ts -g "one three-session hop chain can enter only one bill"` with `E2E_RUN_ID=20260825102302`; the runner confirmed production access disabled and retries `0`.

The run created the intended three-session chain for customer `QA Multi Hop Race 20260825102302`: `session-ad5159ce-3f7d-4e4f-96ac-aa8f79f1b5d5`, `session-2b5832be-9449-41fd-804e-1088649a3d55`, and `session-eb346bf5-40fe-48b4-8f8e-10c2377378be`. It then failed once without retry at `tests/e2e/staging/support/app.ts:229` with `route.abort: Route is already handled!`; failure screenshot attachment also arrived after test end. The evidence attachment recorded `raceStarted = false` and `raceResolved = false`. The intended pair of rewritten race commands was therefore never deliberately submitted, so this execution is not accepted as a product concurrency pass or failure.

While the test held the original browser request, the application's bounded ambiguous-response recovery reconciled one committed UI mutation: `financial-02c5adab-0a9e-4c8e-b3f4-a97cefec3b9f`. Read-only execution-window and exact-ID reconciliation proved exactly one issued bill `bill-9aba945f-d526-4e79-950a-6f6b8d2b10b5` / `BILL-20260825-001`, one cash payment `payment-9b980d72-4e86-43a1-9088-cb39c0aeceea`, one checkout event `event-3a59a985-64bf-4160-8202-bc676b9d8c87`, and three exact Rs 15 session-charge lines. Total, paid, and due were Rs 45, Rs 45, and Rs 0. All three sessions were closed/billed to that same bill; all actors matched authenticated user Vipin (`61cc2f83-69d1-46ab-9d89-9df7f7b1e497`); database duration was `199.081 ms`; and the floor contained zero open sessions/tabs. No second mutation, bill, payment, or checkout event existed.

Reusable inspector artifact `test-artifacts/reconciliation/staging-sessions-multihop-playwright-20260825102302-postfail.json` independently confirmed the exact three sessions, bill lines, actor attribution, and empty floor. The two successful hop operations advanced compatibility version `625` to `627`; the v2 checkout left `app_state` unchanged. Application-canonical postflight hash was `a146331e891123bec05f4d00af9dbdc31c8a2f31ff5398aaeda33c3a5ebb3c45`; PostgreSQL's different JSON serialization produced `5fcdf62b583cdbabe7897970d9eb610eab14bd1fe95bb47a52b31c5da1da8a26`. Reusable postflight `multihop-playwright-20260825102302-postfail` passed with the same version/application hash and zero open sessions/tabs.

The harness root cause was sequential pre-race reads holding intercepted browser requests long enough for bounded recovery to handle the original route, followed by cancellation trying to abort an already-handled route. Independent checkpoint review rejected an intermediate correction because it allowed writes to race the role/state/timing reads and could leave an uncaptured interceptor unsettled. The revised local correction captures authenticated request metadata without blocking, completes and asserts both active-admin roles plus exact application-state and normalized/raw/compatibility timing before installing either checkout interceptor, synchronously validates both captured commands, and then submits them immediately. Interceptor disposal now settles both captured and never-captured paths, and abort cleanup ignores only the exact already-handled-route condition. Its focused contract passes 22/22; the complete local suite passes 39 files / 448 tests, the production build passes, lint has zero errors and the same five warnings, and `git diff --check` passes. Independent final review issued GO to commit this harness checkpoint. A new staging execution requires separate authorization, a fresh run ID, zero retries, and complete mutation/bill/payment/event reconciliation; none of the reconciled `20260825102302` identities may be reused. Production was not accessed and overall Release B remains production NO-GO.

## Fresh authorized multi-hop attempt stopped before financial submission and cleaned safely

The user separately approved one fresh zero-retry execution after the preceding harness checkpoint. Preflight `multihop-rerun-20260825104947` passed at `2026-08-25T05:20:12.769Z` against staging project `tkbdyzxwwbhkpztgjjxh`: authenticated admin actor, zero open sessions/tabs, compatibility version `627`, and application-canonical hash `a146331e891123bec05f4d00af9dbdc31c8a2f31ff5398aaeda33c3a5ebb3c45`. The runner again confirmed bundle `/assets/index-7AYwZnhZ.js` with SHA-256 `30971d9e1b242d6d210d2bbf3732b9dad1d310da470e6e1558252745f47a71ea`, production access disabled, and retries `0`.

Run `20260825104947` created exact sessions `session-70762784-91ba-450b-9682-d28a1f9a912e`, `session-cdc9e3b4-bebb-4f9b-8311-63c9b764c449`, and `session-acb0943e-6c90-428a-9863-ada59859e6c1` for `QA Multi Hop Race 20260825104947`. It then failed once at the pre-send assertion `observer authenticated staging request`; `raceStarted` and `raceResolved` both remained false, neither checkout interceptor was installed, and no financial command or candidate mutation/bill identity was created. The observer browser had authenticated Supabase REST reads but no POST RPC, while the metadata collector watched only POST RPC requests.

The test's guarded cleanup rejected the final session once using mutation `op-b0fff3f7-8210-42c1-8b62-7b2af8fcf486`, event `event-c245e05b-48c1-4ac8-8900-af75f06ecc2c`, audit `audit-8920c200-da24-4431-989c-c0619d1ee2ba`, and exact reason `Playwright pre-race multi-hop cleanup 20260825104947`. Read-only inspector artifact `test-artifacts/reconciliation/staging-sessions-multihop-rerun-20260825104947-postfail.json` proved the two prior sessions remained closed/hopped, the final session was closed/rejected, both prior continuation links were released, every actor matched Vipin, every `closed_bill_id` was null, and zero bill lines referenced any exact session. Compatibility advanced `627` to `630` only through two successful hops and the guarded rejection; final application-canonical hash was `711834767f21dc281e2dbdb0431ba39347408d0a93c667f8d517f43269ad55d7`. Postflight `multihop-rerun-20260825104947-postfail` passed at `2026-08-25T05:21:59.709Z` with zero open sessions/tabs and the same exact baseline.

The local correction broadens the reusable metadata collector to authenticated `/rest/v1/` traffic, safely treats request bodies as optional, and derives the canonical REST base from URL structure instead of assuming an RPC path. It does not alter product code, checkout commands, or the already reviewed pre-send gate and interception lifecycle. Focused harness contracts pass 22/22 and Playwright discovery passes. Independent review issued GO for this collector-only checkpoint and confirmed that no product deployment is required. This attempt is not a product concurrency pass or failure, was not retried, and consumed its authorization. Any future execution requires a fresh run identity, separate staging-write authorization, clean-floor/version-`630` preflight, zero retries, and the same exact post-run reconciliation. Production was not accessed and overall Release B remains production NO-GO.

## Schema-invalid profile preflight stopped a fresh run before financial submission

The user approved one further fresh zero-retry execution. Preflight `multihop-rerun-20260825114908` passed at `2026-08-25T06:19:31.278Z` with staging project `tkbdyzxwwbhkpztgjjxh`, authenticated admin actor, zero open sessions/tabs, compatibility version `630`, and application-canonical hash `711834767f21dc281e2dbdb0431ba39347408d0a93c667f8d517f43269ad55d7`. The runner confirmed the same reviewed staging bundle/hash, production access disabled, and retries `0`.

Run `20260825114908` created exact sessions `session-aeba8ff0-b182-4a39-807d-caeb1a3ebadd`, `session-bfd4d2d0-1afe-4a2b-a9c1-51a92ba4d7fe`, and `session-c99a419e-cbc3-4d36-a1bd-60ecba910be6` for `QA Multi Hop Race 20260825114908`. It then stopped once at the pre-send profile reconciliation with HTTP 400. `raceStarted` and `raceResolved` remained false, neither checkout interceptor was installed, and no checkout mutation, candidate bill, or financial write was created. The harness incorrectly selected `organization_id` from `profiles`; the verified schema stores only identity/global role/activity there, while tenant membership lives in `organization_members` and is authoritatively resolved by `current_user_org_role`.

Guarded cleanup rejected the final session once with mutation `op-e43c32e6-d428-4614-9ae5-c97dd4ae8fa2`, event `event-849450ac-f82d-44b2-9570-92c0f37d5c68`, audit `audit-b232e6e6-4ca3-4b61-ba82-9a5bb996ba9b`, and exact QA reason. Read-only artifact `test-artifacts/reconciliation/staging-sessions-multihop-rerun-20260825114908-postfail.json` proved both prior sessions closed/hopped, the final session closed/rejected, both continuation links released, all actors Vipin, all `closed_bill_id` values null, and zero linked bill lines. Compatibility advanced `630` to `633` only through two successful hops and the rejection; final application-canonical hash was `ab9a311706dda6cb2fa71178ebfc90764da47f68e855d5d9be6660b882a2fd2d`. Postflight `multihop-rerun-20260825114908-postfail` passed at `2026-08-25T06:21:19.038Z` with zero open sessions/tabs and the same exact baseline.

The local harness correction now selects only `id,role,active` from `profiles` and uses the hard-locked staging organization `org-primary` solely as the target for both authenticated `current_user_org_role` checks. Independent checkpoint review rejected an intermediate environment-default implementation because a different non-empty organization override could diverge from the reconciliation scope. The final guard fails during discovery if `E2E_V2_ORGANIZATION_ID` is anything other than `org-primary`; an explicit wrong-organization discovery check failed closed with the exact lock error and zero discovered tests. It changes no product code and requires no deployment. Focused contracts pass 22/22, and independent final review issued GO for the local checkpoint with no remaining static blocker. This attempt was not retried, does not prove or disprove the product race, and consumed its authorization. A future run requires separate staging-write approval, fresh identities, clean-floor/version-`633` preflight, zero retries, and exact reconciliation. Production was not accessed and overall Release B remains production NO-GO.

## Normalized-only primary session stopped a fresh run before financial submission

The user authorized one further fresh zero-retry run. Preflight `multihop-rerun-20260825120115` passed at `2026-08-25T06:31:31.592Z` with staging project `tkbdyzxwwbhkpztgjjxh`, authenticated admin actor, zero open sessions/tabs, compatibility version `633`, and application-canonical hash `ab9a311706dda6cb2fa71178ebfc90764da47f68e855d5d9be6660b882a2fd2d`. The runner confirmed the reviewed staging bundle/hash, production access disabled, and retries `0`.

Run `20260825120115` created sessions `session-6bc347fa-134d-47c8-9012-7d1db4974809`, `session-490dd5a4-5b9b-4bcd-8dac-4b1b52a6a4ef`, and `session-e01f7e72-6310-412b-b45e-4c4f41fe9427` for `QA Multi Hop Race 20260825120115`. It then stopped once at the pre-send assertion that required the normalized-only primary session to exist in compatibility `app_state`. `raceStarted` and `raceResolved` remained false, neither checkout interceptor was installed, and no financial identity or command was created.

Read-only source and database evidence establishes that this was a harness expectation error, not a parity defect: normalized `start_session` does not update `app_state`, while each `hop_session` deliberately patches the now-carried session into compatibility state using its typed timing. Thus the two carried sessions were valid compatibility consumers and the live primary session was correctly absent. Guarded cleanup rejected the final session once with mutation `op-3fb67528-a030-4ca0-8a33-b1e797b1e07d`, event `event-92442524-6ece-4c45-a7a8-6fce224e7c21`, audit `audit-a33c4657-dc41-4477-8698-8cdc1e8713b3`, and the exact QA reason; its server duration was `2865.286 ms`.

Inspector artifact `test-artifacts/reconciliation/staging-sessions-multihop-rerun-20260825120115-postfail.json` proved the two prior sessions closed/hopped, the primary closed/rejected, both continuation links released, all actors Vipin, every `closed_bill_id` null, and zero linked bill lines. Compatibility advanced `633` to `636` only through the two hops and rejection, with final application-canonical hash `e54b6b8d2a5f4bf7a60866590bffed00c5a50b1fc10150d180aa8725c7e19d9e`. Postflight `multihop-rerun-20260825120115-postfail` passed at `2026-08-25T06:33:32.178Z` with zero open sessions/tabs and the same exact baseline.

The local correction validates typed/raw start timing for all three normalized rows, validates compatibility multiplicity and timing only for the two carried/hopped sessions, and explicitly requires the normalized-only primary session to be absent from compatibility. Its focused contract also proves the source SQL distinction: `start_session` has no `app_state` update, while `hop_session` patches and updates the compatibility row. Focused tests pass 22/22 and Playwright discovery passes. Independent final review issued GO and confirmed that the narrower compatibility assertion cannot hide a missing carried session. This run was not retried, does not prove or disprove the product race, and consumed its authorization. A future run requires separate staging-write approval, fresh identities, clean-floor/version-`636` preflight, zero retries, and exact reconciliation. Production was not accessed and overall Release B remains production NO-GO.

## Three-session multi-hop double-bill race passed

The user authorized one fresh execution of the corrected exact test. Preflight `multihop-rerun-20260825122028` passed at `2026-08-25T06:50:45.928Z` with staging project `tkbdyzxwwbhkpztgjjxh`, authenticated admin actor, zero open sessions/tabs, compatibility version `636`, and application-canonical hash `e54b6b8d2a5f4bf7a60866590bffed00c5a50b1fc10150d180aa8725c7e19d9e`. The runner locked the execution to the reviewed staging bundle `/assets/index-7AYwZnhZ.js` / SHA-256 `30971d9e1b242d6d210d2bbf3732b9dad1d310da470e6e1558252745f47a71ea`, production access disabled, one worker, and retries `0`.

Run `20260825122028` passed once in `24.726 s`. It created exact sessions `session-4041b2c2-49fc-4e42-8b01-42e82c717ab4`, `session-f9e3bb4f-74b0-45c0-8abd-732d54c781ca`, and `session-b380f4ae-b5e4-479b-8ba9-47c5095a3fdd` for `QA Multi Hop Race 20260825122028`. Both admin browser contexts completed authoritative role, normalized/raw timing, and compatibility-boundary checks before one captured checkout command per context. The commands used distinct mutation, bill, and bill-number identities and were each submitted exactly once; capture counts remained `[1, 1]` and no winner replay occurred.

Responses were HTTP `[400, 200]`. Loser `financial-multihop-20260825122028-a` returned `session_not_billable`; its mutation-status lookup was null and its candidate bill `bill-eb56ab4d-a14e-4a3c-b2a8-319e6b8f4520`, event, and audits were absent. Winner `financial-multihop-20260825122028-b` committed bill `bill-1b0dcd54-0f16-448a-b22c-47eec44b215c` / `BILL-QA-MULTIHOP-20260825122028-B`, payment `payment-59566652-6f27-4270-ac9f-2cafe01b33d0`, and event `event-b538f345-f42a-41ce-bd51-6896765eca4d`. Server duration was `149.142 ms` (`145.936 ms` core).

The canonical cash bill is Issued with total/paid/due `Rs 30 / Rs 30 / Rs 0` and exactly three Rs 10 session-charge lines, one for each exact session. All three normalized sessions are closed/billed to the same winning bill. The winner has exactly one committed mutation, one payment, one event, and four checkout audits; the loser has none. Every persisted actor equals Vipin (`61cc2f83-69d1-46ab-9d89-9df7f7b1e497`). Compatibility advanced `636` to `638` only through the two pre-race hop operations, then remained exactly version/hash `638` / `452e985c728675c14e80d3e48d1da019d84a055317473b19c89c89d94f903809` across the v2 financial race.

The passing Playwright evidence reported no pending operational mutations, no dialogs or console errors, the single expected loser page error, and both browsers reloaded with the station Available. Independent read-only inspector `multihop-rerun-20260825122028-passed` confirmed all exact sessions, audit/event history, bill links and three lines. Final postflight `multihop-rerun-20260825122028-postpass` passed at `2026-08-25T06:52:13.813Z` with zero open sessions/tabs and the same exact compatibility version/hash. Independent final evidence review issued GO to close the demonstrated admin three-session multi-hop double-billing contention subgate only. Production was not accessed; the remaining Release B gates and overall production NO-GO are unchanged.

## Receptionist-manager role matrix stopped on a zero-total harness assertion

The user authorized one staging-only lifecycle for dedicated receptionist and manager QA accounts and one zero-retry seven-case role matrix. The account-management Playwright lifecycle was independently reviewed before staging. Two provisioning attempts stopped before any user write: the first used the standalone five-second dashboard wait, and the second used an inaccessible exact-label selector for the role control. Both attempts authoritatively proved both deterministic candidate usernames absent and removed the recovery file. After the bounded login wait and the uniquely scoped Create User form selector were independently approved, the third attempt created `qa_role_rec_role_matrix_20260825_1457` and `qa_role_mgr_role_matrix_20260825_1457`. Their authenticated actor IDs were respectively `c62a61c0-5bfa-4e85-9309-d60d51d9dc01` and `b614c4d5-b920-420d-9ae7-a6df7cc2bd49`; authoritative roles were receptionist and manager. Passwords were stored only in ignored `.env.e2e.roles.local` and were never printed or attached.

Read-only preflight `role-matrix-post-provision-20260825-1502` passed at `2026-08-25T09:32:39.041Z` against staging project `tkbdyzxwwbhkpztgjjxh`: zero open sessions/tabs and exact compatibility version/hash `638` / `452e985c728675c14e80d3e48d1da019d84a055317473b19c89c89d94f903809`. The runner then confirmed bundle `/assets/index-7AYwZnhZ.js` / SHA-256 `30971d9e1b242d6d210d2bbf3732b9dad1d310da470e6e1558252745f47a71ea`, production disabled, one worker, and retries `0`.

Run `role_matrix_20260825_1457` passed its distinct active-role preflight and submitted the first case once. Receptionist checkout mutation `financial-7591b170-eb85-4fcf-a981-f48a74ef621e` returned HTTP 200 and committed bill `bill-a19bcf72-5fad-4462-80ab-53f555e41b51` / `BILL-QA-ROLE-ROLE_MATRIX_20260825_1457-REC-CHECKOUT-FIRST-RACE`; the competing manager hop returned HTTP 400 `session_not_open` with no hop event or audit. The test then failed because it required one payment row. Five later cases did not run and were not retried.

Exact admin and same-actor read-only artifacts `staging-sessions-role-matrix-failure-20260825-1457.json` and `staging-sessions-role-matrix-failure-actor-20260825-1457.json` prove that the product result was correct. Session `session-1726613b-c4ea-4faf-853d-9a0862c9743c` ran for approximately four seconds, closed billed to the exact bill, and produced one session line with canonical subtotal `Rs 0.2358888889`. Rounding contributed `Rs -0.2358888889`, so bill total/paid/due were `Rs 0 / Rs 0 / Rs 0`; canonical payments were correctly empty. The bill's retained `issued_by_user_id` and the captured session audit rows match the receptionist actor. The retained event row proves the checkout event identity but does not include its actor field, and the inspector did not select the separate bill-entity audit actor. The actor-scoped status lookup returned the same canonical bill and empty payment result. The financial checkout left compatibility version/hash unchanged.

Fresh postflight `role-matrix-post-failure-20260825-1457` passed at `2026-08-25T09:36:36.730Z` with zero open sessions/tabs and the same exact compatibility version/hash. Independent review classified the failure as a test-contract defect, confirmed that no cleanup bill or retry was appropriate, and issued GO to deactivate the QA accounts. Both exact profiles were disabled, not deleted, preserving attribution; the ignored credential file was removed and no password was printed.

The local harness now accepts zero payment rows only when the bill total is zero, still requires one payment for the positive cash case, preserves exact payment-sum reconciliation, and includes payment actors conditionally. Final local verification passed 25 focused contracts, 39 files / 452 tests, focused Playwright TypeScript no-emit, the production build, lint with zero errors and the same five warnings, and `git diff --check`. This staging authorization is consumed. The matrix remains incomplete because five cases did not run; a future execution requires separate approval, fresh account/run identities, a clean-floor preflight, zero retries, and full exact reconciliation. Production was not accessed, and overall Release B remains production NO-GO.

## Second role matrix run proved both checkout-first outcomes and stopped on an alert-order assertion

The user authorized one fresh lifecycle with run ID `role_matrix_20260825_162048`. Preflight `role-matrix-pre-20260825-162048` and post-provision preflight `role-matrix-post-provision-20260825-162048` both proved exact staging project `tkbdyzxwwbhkpztgjjxh`, zero open sessions/tabs, and compatibility version/hash `638` / `452e985c728675c14e80d3e48d1da019d84a055317473b19c89c89d94f903809`. Dedicated profiles `qa_role_rec_role_matrix_20260825_162048` and `qa_role_mgr_role_matrix_20260825_162048` authenticated as distinct actors `18163795-0a31-41f1-ab84-ff95cf304adf` and `7d855702-009a-4633-9554-e24fb77a4bad` with authoritative receptionist and manager roles. Credentials remained only in the ignored lifecycle file and were not printed.

The exact zero-retry matrix passed role preflight and `rec-checkout-first`. `mgr-checkout-first` then completed all transaction, mutation, actor, bill, audit, conflict, reload, Synced, and Available assertions with `raceReconciled = true` and `terminalBillConfirmed = true`, but failed its final native-dialog assertion. The losing-hop browser retained only the earlier known sync-guard alert; the separately expected refreshed-conflict alert did not arrive before reload destroyed the delayed native alert. Four hop-first/concurrent cases did not run, and no case was retried.

The receptionist winner committed mutation `financial-7f2c0713-8058-4cb0-a7e4-acf5b3624fcc`, session `session-c17eb445-eca8-4a4f-8f2f-d3e9360c4936`, and bill `bill-d3f527b1-63b8-49cd-95ad-86a2bc2a3cd3`. The manager winner committed mutation `financial-f64d79a0-77c3-48f1-a9df-570d5df96939`, session `session-a980826c-ea4f-44ff-9cbd-82838bd79dfa`, and bill `bill-b781c0df-98fe-462a-9476-8fa0cd4c1cd7`. Both stale hops returned HTTP 400 `session_not_open`; neither created a hop event or audit. Both very short timed sessions rounded to issued bills with total/paid/due `Rs 0 / Rs 0 / Rs 0`, one linked session-charge line, and no payment, as required.

Admin reconciliation `staging-sessions-role-matrix-reconcile-admin-20260825-162048.json` and actor-scoped reconciliations `staging-sessions-role-matrix-reconcile-rec-20260825-162048.json` / `staging-sessions-role-matrix-reconcile-mgr-20260825-162048.json` proved exactly two closed/billed sessions, two bills, two checkout events, zero payments, matching canonical mutation results, correct bill and captured session-audit actors, and unchanged compatibility state. Final postflight `role-matrix-post-failure-20260825-162048` passed at `2026-08-25T10:54:26.612Z` with zero open sessions/tabs and the same exact version/hash. Independent review classified the stop as a test-contract/UI synchronization failure, not a transaction defect, and approved deactivation. Both exact profiles are inactive; the ignored credential file was removed without printing passwords.

The local assertion now retains the stronger deterministic HTTP, database, visible `1 conflict`, Clear, Synced, and Available proofs; it permits only the known sync-guard and optional refreshed-conflict native alerts and rejects every unexpected message. This second authorization is consumed. The two checkout-first role directions are demonstrated, but the four hop-first/concurrent cases remained open at this checkpoint. Production was not accessed, and overall Release B remains production NO-GO.

## Remaining-only role matrix proved receptionist checkout versus manager hop-first

The user authorized one fresh lifecycle restricted to the four then-unexecuted hop-first/concurrent cases. The fail-closed runner added exact `--remaining` and `--remaining-list` modes; discovery showed only the role preflight plus those four cases, rejected extra or unknown arguments, and never forwarded the mode as a Playwright path. Run ID `role_matrix_remaining_20260825_165519` used dedicated profiles `qa_role_rec_role_matrix_remaining_20260825_165519` and `qa_role_mgr_role_matrix_remaining_20260825_165519`, authenticated as distinct receptionist actor `1abb25eb-c0a1-4485-a26c-38949b703f0f` and manager actor `7c484140-d643-41d1-9199-7285d1680a03`. Credentials existed only in the ignored lifecycle file and were never printed.

Preflight `role-matrix-remaining-pre-20260825-165519` and post-provision preflight `role-matrix-remaining-post-provision-20260825-165519` both proved exact staging project `tkbdyzxwwbhkpztgjjxh`, zero open sessions/tabs, and compatibility version/hash `638` / `452e985c728675c14e80d3e48d1da019d84a055317473b19c89c89d94f903809`. The exact zero-retry remaining-only command selected five tests with one worker against bundle `/assets/index-7AYwZnhZ.js` / SHA-256 `30971d9e1b242d6d210d2bbf3732b9dad1d310da470e6e1558252745f47a71ea`; production targeting was false.

The role preflight passed and `rec-checkout-hop-first` submitted each captured command once. Manager hop mutation `op-a4dd4ad2-84e8-41c0-8f72-b8c605560fa8` returned HTTP 200, closed session `session-8b74ecc6-e22d-4b64-85b2-fcf79b3e0164` as hopped, and advanced compatibility state from version `638` to `639`. The stale receptionist checkout returned HTTP 400 `invalid_session_timing`; mutation `financial-6f6ed0d2-70cb-42be-becd-4a875059f46a` remained absent and created no bill, payment, checkout event, or checkout audit.

The manager then used the guarded `Bill & Done` cleanup once with the unchanged normalized start/end instants. Mutation `financial-75c62495-26f3-457f-bcbb-6539befb2129` committed bill `bill-381df81c-00c9-45a2-b7ca-21f68f409e56` and one linked session-charge line. Its canonical subtotal was `Rs 0.2838333333`; rounding produced total/paid/due `Rs 0 / Rs 0 / Rs 0`, so canonical payments were correctly empty. The test stopped on the obsolete unconditional expectation of one cleanup payment; the manager-hop-first and both concurrent cases did not run and no test was retried.

Actor-scoped reconciliation `staging-sessions-role_matrix_remaining_20260825_165519_manager.json` captured the committed cleanup mutation before deactivation. Final admin actor reconciliation `staging-sessions-role_matrix_remaining_20260825_165519_admin_actor_final.json` proved the session closed/billed to the exact bill, one linked line, zero payments, manager attribution on the hop event/audit and checkout event/bill/audit, and compatibility version/hash `639` / `d416ab4c7ca24df5fbaa743265cdac97201256426aad20edfa6cf4e2755cfad7`. Thus the hop alone advanced compatibility state and the v2 cleanup did not rewrite it. Postflight `role-matrix-remaining-postflight-20260825-165519` passed at `2026-08-25T11:30:23.654Z` with zero open sessions/tabs and the same exact version/hash.

Independent review classified the stop as a harness-contract defect, not a product defect, approved the conditional zero/one payment assertion, and authorized deactivation without further cleanup or retry. Both exact profiles are inactive and the ignored credential file was removed without printing passwords. The reusable inspector now supports exact audit IDs and includes operational-event actors; the cleanup assertion expects a payment only when `amount_paid > 0`, still reconciles the full payment sum, and conditionally checks payment actors. This authorization is consumed. The matrix has three open cases: manager checkout versus receptionist hop-first and both role directions under simultaneous submission. Production was not accessed, and overall Release B remains production NO-GO.

## Final three ordinary-role checkout-hop cases passed

The user authorized one fresh staging-only lifecycle for exactly the three unexecuted cases. A fail-closed `--remaining-three` selector and read-only list variant were added; discovery listed only the shared active-role preflight plus `mgr-checkout-hop-first`, `rec-checkout-concurrent`, and `mgr-checkout-concurrent`. Multiple or unknown arguments failed closed, and the selector was not forwarded as a Playwright path. Focused contracts passed 25/25, focused TypeScript no-emit passed, the complete local suite passed 39 files / 452 tests, and independent pre-run review issued GO.

Run `role_matrix_final_20260825_2249` used fresh profiles `qa_role_rec_role_matrix_final_20260825_2249` and `qa_role_mgr_role_matrix_final_20260825_2249`, authenticated as distinct receptionist actor `c169c04a-76ff-469f-94a2-9b0484ce6512` and manager actor `b0cbd2e5-7086-4d4d-9c7a-59982e5a142a`. Preflight `role-matrix-final-pre-20260825-2249` and post-provision preflight `role-matrix-final-post-provision-20260825-2249` both proved exact staging project `tkbdyzxwwbhkpztgjjxh`, zero open sessions/tabs, and compatibility version/hash `639` / `d416ab4c7ca24df5fbaa743265cdac97201256426aad20edfa6cf4e2755cfad7`. Credentials remained only in the ignored lifecycle file and were never printed.

The exact zero-retry run passed all four selected tests once in `1.1m` with one worker against bundle `/assets/index-7AYwZnhZ.js` / SHA-256 `30971d9e1b242d6d210d2bbf3732b9dad1d310da470e6e1558252745f47a71ea`; production targeting was false. Each checkout and hop was captured and sent exactly once. In all three naturally timed cases the hop returned HTTP 200 and the stale ordinary-staff checkout returned HTTP 400 `invalid_session_timing`; each losing checkout mutation remained null and created no race bill, payment, checkout event, or checkout audit.

Guarded `Bill & Done` cleanup then billed each exact hopped session once using unchanged normalized start/end instants. Session `session-ad0c7144-5519-4d4d-b2dd-389207b43753` closed to receptionist-attributed bill `bill-149598f9-a6ea-4431-9e13-86dc5ab67e12` with total/paid/due `Rs 0 / Rs 0 / Rs 0` and zero payments. Session `session-c7f7aba0-b1e7-4400-8c9d-1a5269816cf4` closed to manager-attributed bill `bill-fb7ddbae-002c-4ff9-aa89-e7292312f42f` with total/paid/due `Rs 1 / Rs 1 / Rs 0` and exactly one manager-attributed Rs 1 cash payment. Session `session-0b0f081c-ded2-4ba1-9c9d-79d8fe0cada5` closed to receptionist-attributed bill `bill-ccf36262-cf6a-443f-8f01-675fdc4caa33` with total/paid/due `Rs 0 / Rs 0 / Rs 0` and zero payments. Every bill has exactly one linked `session_charge` line.

Role-scoped inspector artifacts `staging-sessions-role_matrix_final_20260825_2249_rec.json` and `staging-sessions-role_matrix_final_20260825_2249_mgr.json` independently confirmed all three terminal session/bill links, payment totals, committed and null mutation results, hop and checkout events, exact hop/bill audits, and actor attribution. Compatibility advanced only through the three hops: `639 -> 640`, `640 -> 641`, and `641 -> 642`; each v2 cleanup left the corresponding version unchanged. Final postflight `role-matrix-final-postflight-20260825-2249` passed at `2026-08-25T17:24:04.558Z` with zero open sessions/tabs and version/hash `642` / `7e6b2b1afe1961a9b2e822012fc90d038ec3e6e2f7021a1635a5c32797ed5dd1`.

Independent post-run review issued GO and closed the ordinary receptionist/manager checkout-hop matrix subgate: together with the prior runs, all six role direction/order combinations are evidenced. Both exact temporary profiles are inactive and the ignored credential file was removed without printing passwords. This authorization is consumed. Production was not accessed, and overall Release B remains production NO-GO pending the other mandatory gates above.

## Checkout versus standalone settlement attempt stopped before race

Independent pre-run review initially returned NO-GO until the reusable case bound both captured commands to one authoritative live pending-bill snapshot, derived both session IDs from acknowledged RPC evidence, required identity-bound cleanup plus exact terminal reconciliation, and linked the winning canonical mutation result to its persisted payment, settlement audit, and event IDs. After those corrections, the independent reviewer authorized exactly one zero-retry staging execution. Final read-only preflight at `2026-08-27T05:32:25.386Z` proved zero open sessions/tabs and compatibility version/hash `642` / `7e6b2b1afe1961a9b2e822012fc90d038ec3e6e2f7021a1635a5c32797ed5dd1`.

Run `20260827053236` targeted the approved staging bundle `/assets/index-7AYwZnhZ.js` / SHA-256 `30971d9e1b242d6d210d2bbf3732b9dad1d310da470e6e1558252745f47a71ea`, with production disabled, one worker, and zero retries. Setup issued deferred `BILL-20260827-001` (`bill-ea56ff7e-6233-46b0-8514-82cb7851e6f6`) for Rs 45 through mutation `financial-77a2c081-c972-4ca2-b2ce-c2ccff0ec112`; it has one linked session-charge line, zero payments, pending/paid/due `pending / Rs 0 / Rs 45`, and the expected authenticated-admin bill/audit/event attribution. The second exact session was started and timing-edited, but its modal remained open. A redundant click on the already-active dashboard navigation was therefore blocked by the modal backdrop and timed out before either race interceptor was installed. Capture counts were checkout `0` and settlement `0`; no checkout-versus-settlement mutation identity, request, payment, audit, event, or outcome exists, so this run provides no concurrency evidence.

The harness did not retry. Guarded cleanup rejected exact session `session-18e088f2-8d95-48af-9cd3-169007080562` through acknowledged event `event-fbb88f1c-acf4-41a5-b0e1-608975cfe057` and audit `audit-f04fdd5b-c48f-4f2c-8444-58f62b9668c3`; exact read-only inspection proved `closed / rejected / closed_bill_id null`. The deferred source session remains correctly `closed / billed` to the pending setup bill. Postflight at `2026-08-27T05:33:55.293Z` proved zero open sessions/tabs and compatibility version/hash `643` / `f0c3300bb4e4c024a994fb41aa1aa266fea4bc91ed498e987687cdecd6174208`; the sole increment is recorded on the cleanup rejection event. Artifacts are `summary-20260827053236.json`, the attached race evidence JSON, `staging-sessions-checkout_settlement_race_20260827053236.json`, and the two pre/postflight JSON files. The redundant navigation was removed locally so the existing session modal proceeds directly to checkout. This staging authorization is consumed; a fresh execution requires separate approval. Production was not accessed, and Release B remains production NO-GO.

Independent post-run review classified this as a harness-contract failure rather than a product defect and issued cleanup GO with one explicitly retained financial artifact: the documented Rs 45 QA receivable must not be altered without separate authorization. The concurrency gate remains open. Any new live attempt requires a fresh explicit approval, unique run identity, and read-only empty-floor/app-state preflight; none of the consumed authorization carries forward.

## Checkout recovery committed after a second pre-race harness stop

The user authorized one fresh staging-only zero-retry execution after the redundant-navigation correction. Read-only preflight passed at `2026-08-27T05:40:48.962Z` against staging project `tkbdyzxwwbhkpztgjjxh`: authenticated admin actor, zero open sessions/tabs, and exact compatibility version/hash `643` / `f0c3300bb4e4c024a994fb41aa1aa266fea4bc91ed498e987687cdecd6174208`. Run `20260827054102` used bundle `/assets/index-7AYwZnhZ.js` / SHA-256 `30971d9e1b242d6d210d2bbf3732b9dad1d310da470e6e1558252745f47a71ea`, production access disabled, one worker, and retries `0`.

The setup issued deferred `BILL-20260827-002` (`bill-d947b425-444e-40b3-80d4-9498faeadd46`) for Rs 30 through mutation `financial-b6d5baa3-bae9-4fcd-8cd6-a05a651cecfc`, with zero payments and one exact session-charge line. The second session and both UI-generated commands were captured once. Before either command was deliberately submitted, a harness assertion compared the full authenticated-header object and failed because the earlier identity capture correctly included an additional `content-type` header. `raceStarted` remained false, so this execution did not perform or evaluate the intended concurrent checkout-versus-settlement submission.

Failure teardown then canceled the held request but removed its route before the browser had fully quiesced. The application's bounded ambiguous-response handling committed the captured checkout mutation `financial-faa907cc-44f8-44cd-a709-9767edcf1427`. It issued `BILL-20260827-003` (`bill-3e1207bf-a5ae-4297-a521-8eb44352add4`) for Rs 30 with cash payment `payment-a100284a-087c-46c9-8d2b-7cc6054ca885`, and settled the prior Rs 30 bill with cash payment `payment-016018b1-00ff-476a-bd3e-896f3acdec60`. Settlement audit `audit-c58db6c6-0027-459e-a72a-6ec61133726b` explicitly links the old bill's settlement to checkout `BILL-20260827-003`; checkout event `event-b62847bf-97a1-4d8d-984e-195c27bcd277` records both payments and both changed bills. Both sessions are closed/billed, both bills are issued with total/paid/due `Rs 30 / Rs 30 / Rs 0`, and all bill, payment, audit, and event actors equal authenticated admin `61cc2f83-69d1-46ab-9d89-9df7f7b1e497`.

No standalone financial-adjustment result, payment, audit, or event exists in the exact run reconciliation; its generated mutation ID was not retained before the failed assertion, so no unsupported exact-ID claim is made. Read-only artifact `test-artifacts/reconciliation/staging-sessions-checkout_settlement_recovery_20260827054102.json` records the two exact sessions, bills, payments, mutations, audits, events, and canonical checkout results. Immediate postflight proved zero open sessions/tabs and unchanged compatibility version/hash `643` / `f0c3300bb4e4c024a994fb41aa1aa266fea4bc91ed498e987687cdecd6174208`. The valid recovered financial rows are retained and were not altered during reconciliation.

This is accepted only as checkout recovery plus previous-dues functional evidence, not as concurrency evidence. The local correction replaces whole-header equality with endpoint and authenticated-subject checks, records both generated mutation identities before subsequent database assertions, keeps interceptors installed until captured requests settle, terminates pre-race recovery while routes are still held, and redacts attached error text. The token-bearing generated Playwright run directory and summary were removed from the workspace before commit after the non-secret exact reconciliation was preserved; a scoped scan found no remaining JWT-bearing Playwright artifact file in the intended result roots. This authorization is consumed. A further staging execution requires fresh explicit approval, a unique run identity, an empty-floor/app-state preflight, zero retries, and independent pre-run review. Production was not accessed, and Release B remains production NO-GO.

Local verification of the corrected checkpoint passes 40 test files / 454 tests, the production build passes with only the existing large-chunk warning, lint exits with zero errors and the same five baseline warnings, `git diff --check` passes, and read-only Playwright discovery lists exactly the one staging-locked race case with retries `0` and production disabled. Independent post-run review issued GO to commit this local harness/evidence checkpoint after narrowing the two evidence statements above; it did not authorize another staging execution or close the concurrency gate.

## Checkout versus standalone settlement race completed

Commits `12f1b59` and `35264b9` added and hardened the exact read-only preflight; commit `7d7192d` added exact before/after reconciliation, distinct-identity reject-only cleanup, and the normalized `active` status correction. Preflight `20260827115031` passed at `2026-08-27T11:50:34.648Z` against staging project `tkbdyzxwwbhkpztgjjxh`. Both independent credential slots resolved to active organization-admin actor `61cc2f83-69d1-46ab-9d89-9df7f7b1e497`; the floor was empty; retained `BILL-20260827-001` was unchanged at pending/paid/due `Rs 45 / Rs 0 / Rs 45` with zero payments; and compatibility was version/hash `643` / `f0c3300bb4e4c024a994fb41aa1aa266fea4bc91ed498e987687cdecd6174208`. Production targeting was false and retries were `0`.

The one authorized race created deferred setup `BILL-20260827-004` (`bill-71c0b842-925c-46be-bb5c-9318ef070964`) for Rs 30 and exact active session `session-61214135-435f-40c1-a2fb-7067e39ba191`. Each UI-generated race command was captured and submitted once. Standalone adjustment `financial-adjustment-ebb59e5e-4564-41ce-aee1-466c90b5900e` won, producing event `event-0f410d09-13f7-4597-8217-9a808edabd69`, cash payment `payment-f82aa5b1-ae87-44ef-90d1-87a50b82148f`, and settlement audit `audit-b1a8d24a-ca96-439f-91dc-4aea694232cb`. The bill finished issued/paid/due `Rs 30 / Rs 30 / Rs 0`; the selected bill/payment/audit/event rows equal the authenticated admin; and `related_checkout_bill_id` is null. Checkout `financial-e670987c-c33b-4355-a970-32f3223c32f7` lost with HTTP 400; the executed response assertion proves `settlement_conflict`, although the retained compact attachment did not copy the response body. Its mutation status is null, candidate `bill-70d9ea97-5eba-40ad-a525-de99ce3d376b` is absent, and no losing payment, event, audit, or line exists. Compatibility remained exactly version/hash `643` / `f0c3300bb4e4c024a994fb41aa1aa266fea4bc91ed498e987687cdecd6174208` across both financial commands.

The Playwright result failed only after this complete outcome because the adjustment-winner branch expected raw session status `open`; the normalized database correctly stores `active`. Independent review classified the mismatch as a harness assertion defect, not a product defect, and no race retry occurred. Exact pre-cleanup reconciliation artifact `test-artifacts/reconciliation/checkout-settlement-race-before-cleanup-20260827115031.json` proves the complete winner/loser result and the unbilled active session. The assertion now accepts `active`.

After separate cleanup approval, exactly one reject-only execution `cleanup_20260827121000` rejected that session without invoking a financial RPC. It produced mutation `op-1b20b65f-6aeb-4b32-a07c-1fecd79b3023`, event `event-77212750-7fda-4ce3-bba2-9044d2bcad0a`, and audit `audit-4bddebed-cf7c-4a48-adec-e646fd94cfeb`, all bound to the exact source/execution identities and authenticated actor. After-cleanup artifact `test-artifacts/reconciliation/checkout-settlement-race-after-cleanup-20260827115031.json` proves the session `closed / rejected / closed_bill_id null`, zero open sessions/tabs, unchanged financial rows and losing absence, retained `BILL-20260827-001` untouched, and compatibility advanced only through the acknowledged rejection to version/hash `644` / `728e42f03b7da0925636606a168ed5964daaf70869cb72cf936e498dee673114`.

A later read-only Playwright discovery mistakenly reused `E2E_RUN_ID=20260827115031` and overwrote only the compact `summary-20260827115031.json` plus `.last-run` metadata. The original race attachment, screenshots, videos, and error context remain in the run directory. `test-artifacts/reconciliation/checkout-settlement-race-runner-summary-20260827115031.json` is explicitly labeled reconstructed and is not represented as the original summary. The preserved run attachment/media plus exact before/after database reconciliations are the primary evidence.

Independent final review issued GO to close only the checkout-versus-standalone-settlement concurrency subgate. This authorization is consumed; no further staging write or rerun is authorized. Production was not accessed and remains NO-GO pending remaining operational/financial writer contention, the broader complex/payment/carryover matrix, production-sized performance acceptance, and final independent release sign-off.

## Checkout versus direct live session-item mutation completed

The initial read-only preflight `session-item-race-202608280908` stopped before any staging write because both credential slots resolved to the same admin actor. A narrow staging-only lifecycle then provisioned one run-bound second admin at a time, bound its actor and ignored credential file to immutable create evidence, retained it through reconciliation and cleanup, and deactivated it with password-free evidence afterward.

Zero-retry runs `session-item-race-202608280924` and `session-item-race-202608280936` each completed checkout-first before a later reload-convergence assertion stopped the remaining orderings. Neither run was retried. Exact recovery artifacts proved one canonical Rs 45 checkout bill, payment, mutation, and financial event in each run; the stale `add_session_item` command returned `session_not_open` and created no session item, reservation movement, audit, or operational event. Identity-bound cleanup runs `session-item-cleanup-202608280927` and `session-item-cleanup-202608280939` archived only their exact fixtures. Corrected immutable postflights proved stock 3, the committed financial rows unchanged, zero open sessions/tabs, and compatibility advancement only from acknowledged fixture administration. Both temporary admins were deactivated and both ignored credential files were removed.

The reusable conflict helper was then corrected to treat an immediately rendered Synced label as provisional: it polls for Clear first and accepts Synced only after a stable 1.5-second dwell, preventing delayed local hydration from restoring an uncleared conflict. A fail-closed remaining-two selector was added so the already-proven checkout-first ordering would not consume another browser run. Full and remaining-two discovery each listed exactly one staging-only, one-worker, retries-zero test with the expected ordered scenario names. Focused harness contracts passed 6/6, and independent pre-run review issued GO.

Read-only preflight `session-item-race-rem2-202608280950` passed with staging project `tkbdyzxwwbhkpztgjjxh`, bundle `/assets/index-CM3_cZEL.js` / SHA-256 `359fbc5e6530a71d1be6a7c7e3399dbcd441c25d2e234e5e3c391e2bab1e69b9`, distinct active admin actors `61cc2f83-69d1-46ab-9d89-9df7f7b1e497` and `7f605935-d716-4986-b52a-0d20b60dc09b`, an empty floor, no run-identity collisions, and compatibility version/hash `666` / `26f69c5f085e6e0986e521d9279aaee96d6a308c60dfea703b6e2e004db81b10`. The exact `item_first + simultaneous` Playwright test passed once in 47.3 seconds with one worker and retries `0`.

In item-first, `add_session_item` committed item `session-item-ae41c1fd-951a-4ad2-b73c-91da249f1c1e`, reservation `stock-9625a0dc-0ee4-4282-ad8c-b8e54772daa8`, and event `event-11eb3a17-e405-49a1-88b5-a1c69126f038`; the stale checkout left no mutation, bill, payment, line, checkout event, or checkout audit. In simultaneous, the item command was again the sole winner, with item `session-item-ad744396-4bef-4f2e-a91e-22f006d61a70`, reservation `stock-f53dffd7-0994-426e-8cd1-d984a2fdd726`, and event `event-cb715235-ddce-4d96-b4d1-e1d6b8fd14e3`; the losing checkout again had a null mutation and zero financial effect. Both exact sessions were then rejected through acknowledged operational cleanup, which released their reservations, and the exact QA fixture was archived.

Mandatory read-only artifact `test-artifacts/reconciliation/checkout-session-item-race-postflight-session-item-race-rem2-202608280950.json` passed with no failures: both sessions are `closed / rejected / closed_bill_id null`, no run bill exists, stock is restored to 3, the fixture is inactive, the floor is empty, and compatibility is version/hash `670` / `fdc6df6ffbf15b986a6bf4fc444258737398c79154244d90b2b4e8a43f7ec398`. The exact temporary admin lifecycle `item-admin-202608280948` ended inactive, its credential file was removed, and no password was printed. Independent final review issued GO to close only this checkout-versus-direct-session-item staging subgate. Together, the prior checkout-first evidence and the final remaining-two pass close all three direct-session-item orderings. Production was not accessed and remains NO-GO pending the remaining release gates.

## Customer-tab source-mutation race stopped after one reconciled ordering

Commits `f823316` and `28dd1a4` added the fail-closed four-mode/three-ordering customer-tab race harness, immutable recovery reconciler, identity-bound cleanup, and independent cleanup postflight. Local verification before execution passed 46 test files / 492 tests, the production build, lint with zero errors and the same five baseline warnings, exact one-test/one-worker/retries-zero Playwright discovery, and independent pre-run review. A first read-only preflight stopped before writes because both credential slots resolved to the same actor. Temporary staging-admin lifecycle `tab-mut-admin-20260828-1754` then created distinct actor `d7ee9be2-44d7-40c8-bf12-d238fe038921`; its credentials remained only in an ignored file and were not printed.

Fresh preflight `tab-mut-race-20260828-1755` passed against project `tkbdyzxwwbhkpztgjjxh`, bundle `/assets/index-CM3_cZEL.js` / SHA-256 `359fbc5e6530a71d1be6a7c7e3399dbcd441c25d2e234e5e3c391e2bab1e69b9`, distinct active admin actors, an empty floor, and compatibility version/hash `670` / `fdc6df6ffbf15b986a6bf4fc444258737398c79154244d90b2b4e8a43f7ec398`. The single authorized zero-retry run executed only `add_item / checkout_first`. Checkout mutation `financial-70f32cb6-399d-464d-af4d-1f55fc306fd8` returned HTTP 200 and issued `BILL-QA-TAB-MUT-tab-mut-race-20260828-1755-add_item-checkout_first` (`bill-17b29eaa-0f8b-4802-aac7-77354730d0fc`) for Rs 50 with one Rs 50 cash payment, one sale movement of -1, authenticated origin attribution, and tab `customer-tab-a8271d89-7775-4e54-a211-7afb53ea6f2d` closed/billed. The stale `add_customer_tab_item` command returned HTTP 400 `customer_tab_not_open` and created no candidate item, audit, event, movement, or compatibility write. Physical fixture stock moved exactly 64 to 63; the financial race left compatibility unchanged at version/hash `672` / `01e4cc5ce3cf85bad148df3eca2daa8e2ab1677aff9084d00c9b9aea60650b7e`.

The test then failed at post-case UI convergence. Screenshots prove the origin was Synced while the observer correctly retained `1 conflict` for the losing operational command; the harness incorrectly waited for both pages to be Synced without first asserting and clearing the expected conflict. No race was retried and the other eleven mode/order combinations did not run. Corrected read-only reconciliation `test-artifacts/reconciliation/checkout-tab-mutation-race-recovery-tab-mut-race-20260828-1755-recon2-20260828-1758.json` returned `reconciled`, zero failures, and `safeForIdentityBoundCleanup: true` after checking the exact bill, line, payment, mutation, event, audit, tab, item, movement, actor, stock, and compatibility sets.

Identity-bound cleanup `tab-mut-cleanup-20260828-1800` passed once with one worker and zero retries. It archived only combo `combo-ad3e9ee3-57f2-4b99-a74b-be2037f00433` and item `inventory-45c35add-9562-45cc-81b2-abb888e6f822`; the two acknowledged admin writes advanced compatibility exactly `672 -> 673 -> 674`. Independent read-only postflight `test-artifacts/reconciliation/checkout-tab-mutation-race-cleanup-postflight-tab-mut-cleanup-20260828-1800-tab-mut-cleanup-postflight-20260828-1802.json` passed with zero failures: both fixtures are inactive, stock remains 63, the complete financial chain is unchanged, and the floor is empty. The temporary admin is inactive and its ignored credential file was removed without printing passwords.

The local harness now follows the established checkout/hop and checkout/reject contract: a checkout winner must show exactly one observer conflict, no pending sync, then Clear and reach Synced; an operational winner still requires both pages Synced. Focused contract checks pass 31/31 and focused lint is clean. Independent post-run review issued GO for local verification and documentation only. This evidence closes only `add_customer_tab_item / checkout_first`; 1 of 12 cases executed and the remaining 11 customer-tab orderings are open. No staging rerun or subgate closure is authorized. Production was not accessed, and overall Release B remains production NO-GO.

A separate local-only follow-up added a fail-closed `remaining-eleven` phase. Runner, preflight, browser checkpoint, recovery reconciler, and postflight bind the original ordered 12-case matrix with exactly `add_item / checkout_first` excluded. The reconciler independently accepts only `all` or that exact ordered 11-case list, treats partial recovery only as its prefix, and requires all 11 for phase completion. Focused checks pass 32/32, Node syntax and focused lint are clean, read-only discovery lists exactly one retries-zero test titled `remaining eleven zero-retry orderings preserve one canonical winner`, and independent review issued local GO. This is readiness evidence only; none of the 11 cases has run.

## Customer-tab source-mutation race: seven additional orderings reconciled

Temporary staging-admin lifecycle `tab-mut-rem11-admin-20260829-1303` created distinct mutation actor `10a2be66-282c-4f0e-971d-bafedaad884f`; origin actor remained `61cc2f83-69d1-46ab-9d89-9df7f7b1e497`. Fresh remaining-eleven preflight `tab-mut-rem11-20260829-1305` passed against staging project `tkbdyzxwwbhkpztgjjxh`, deployed bundle `/assets/index-CM3_cZEL.js` / SHA-256 `359fbc5e6530a71d1be6a7c7e3399dbcd441c25d2e234e5e3c391e2bab1e69b9`, an empty floor, no run-identity collisions, and compatibility version/hash `674` / `ed24be3139a09f478546b7c9faf5762cbb41c3612c1fd06fd0f8b6562c430e46`.

The one-worker, retries-zero Playwright execution ran once and was not retried. It reached the global 120-second timeout while preparing `remove_item / simultaneous`, before that case submitted either race command. The immutable checkpoint therefore contains exactly eight started entries: seven fully completed races and one prepared-only tab. The seven complete cases are `add_item / mutation_first`, `add_item / simultaneous`, all three `update_item` orderings, and `remove_item / checkout_first` plus `remove_item / mutation_first`. Corrected read-only reconciliation `tab-mut-rem11-recon2-20260829-1310` returned `reconciled`, `failures: []`, and `safeForIdentityBoundCleanup: true`; it treats the eighth entry as incomplete rather than inferring a winner.

Two of those seven races were checkout winners. `update_item / checkout_first` issued bill `bill-1e3f8b7d-439c-472b-852c-af193b3211ac`, and `remove_item / checkout_first` issued bill `bill-433ba5e0-548d-407e-8b13-3b517770f3cf`; each is Rs 50 with exactly one Rs 50 cash payment and one sale movement of -1 attributed to the authenticated checkout actor. The other five cases were operational winners: add mutation-first/simultaneous, update mutation-first/simultaneous, and remove mutation-first. Each retained its exact mode-specific canonical row/event/audit contract, the losing financial mutation stayed null with zero bill/payment/movement effect, physical stock did not change through the operational command, and its open tab was later rejected by acknowledged harness cleanup. Compatibility after the seven fully handled cases and the prepared eighth tab was version/hash `681` / `727c269c14e07ea6afae00aba0c5b11e5ef4a745fdd9428ef7b335cf1775486e`.

Identity-bound cleanup `tab-mut-rem11-cleanup-20260829-1312` rejected only the prepared eighth tab and archived the exact combo, advancing compatibility `681 -> 682 -> 683`. Its subsequent item archive returned HTTP 400 because the page still held the pre-combo compatibility version; the UI displayed the canonical stale-data guard. The run was not retried. Fresh read-only recovery proved the item remained active at unchanged stock 62 with null archive fields, no item archive event/audit/movement, the combo inactive, the floor empty, all bills/payments/mutations unchanged, and exact version/hash `683` / `8146a2cb65ee39fbb4ab309d978d17666e55dc6f94717a2a8dec4ec11fdbd7ae`.

The first purpose-built partial-cleanup authorization artifact (`tab-mut-rem11-item-recovery-20260829-1325`) blocked only on two evidence-shape mismatches and did not authorize a write. After independently reviewed field-by-field normalization, fresh read-only artifact `tab-mut-rem11-item-recovery-20260829-1327` returned `item-cleanup-authorized`, `failures: []`, and `safeForItemOnlyCleanup: true`, with allowed actions exactly `[archive_item]`. One fresh zero-retry item-only cleanup `tab-mut-rem11-item-cleanup-20260829-1330` passed. Mandatory postflight `tab-mut-rem11-item-postflight-20260829-1332` then returned `passed`, proving the item inactive at stock 62, exact archive event `event-21c00606-2889-45e0-ba9c-f38b3c2286f7`, audit `audit-7d8e2966-9d8d-408f-a0d8-d7dbc5229096`, correct actor/reason, no added movement, unchanged prior financial/operational evidence, empty floor, and the sole final compatibility increment to version/hash `684` / `63d2f9093b869f2b3bd128f9d2ff1f2c830cffbc654a8769f37a9266afbd1350`. The temporary admin is inactive, its ignored credential file is removed, and no password was printed.

Together with the prior `add_item / checkout_first` run, eight of twelve customer-tab mutation orderings are now evidenced. Four remain open: `remove_item / simultaneous` and all three `apply_combo` orderings. A new fail-closed `remaining-four` selector binds exactly those ordered cases across runner, preflight, browser checkpoint, reconciliation, and postflight. Focused selector/harness tests pass 32/32 and read-only discovery lists exactly one one-worker/retries-zero test titled `remaining four zero-retry orderings preserve one canonical winner`. Final local verification passes 46 test files / 493 tests, the production build with only the known large-chunk warning, lint with zero errors and the same five baseline warnings, and `git diff --check`. No new remaining-four staging race has been run or authorized by this checkpoint. Production was not accessed, and overall Release B remains production NO-GO.

## Customer-tab source-mutation race completed

Temporary staging-admin lifecycle `tab-mut-rem4-admin-20260829-1336` created distinct mutation actor `098dd516-1451-4489-9383-8cf64d2055d4`; checkout actor remained `61cc2f83-69d1-46ab-9d89-9df7f7b1e497`. Read-only preflight `tab-mut-rem4-20260829-1338` passed against staging project `tkbdyzxwwbhkpztgjjxh`, deployed bundle `/assets/index-CM3_cZEL.js` / SHA-256 `359fbc5e6530a71d1be6a7c7e3399dbcd441c25d2e234e5e3c391e2bab1e69b9`, distinct active actors, an empty floor, zero run-specific catalog/tab/bill/artifact collisions, and compatibility version/hash `684` / `63d2f9093b869f2b3bd128f9d2ff1f2c830cffbc654a8769f37a9266afbd1350`. Independent review issued GO for exactly the fail-closed four-case selector.

The one-worker, retries-zero run passed once in about 1.3 minutes. `remove_item / simultaneous` was an operational winner: exact removal event `event-668312b7-8aea-4315-8bbe-d61dfaa5d312` and audit `audit-484e786c-c572-446a-94d2-905c23bb9536` belong to the mutation actor, while checkout mutation `financial-142ef97b-838b-4647-b495-8aeb63a4da80` is null and created no bill, payment, event, audit, or movement. `apply_combo / checkout_first` was the sole checkout winner: mutation `financial-9cdfce70-3da7-4e78-9b2a-9ff34bba34d6` issued Rs 50 cash bill `bill-78fa2bb2-ded7-4ed7-b9c7-e52178cdd490`, payment `payment-4162ae5c-6215-4ea8-bc2d-f55e54cfd9a0`, and sale movement `stock-12e070af-689f-4f86-9163-dbae9f51c7ab` of `-1`, all attributed to the checkout actor; the stale combo command had zero operational effect. `apply_combo / mutation_first` and `apply_combo / simultaneous` were operational winners with their exact combo-application rows, item snapshots, events, and audits attributed to the mutation actor; both checkout mutations are null with zero financial effect. Thus the run contains exactly one bill, one line, one payment, and one sale movement, with no duplicate effect.

Mandatory read-only reconciliation `test-artifacts/reconciliation/checkout-tab-mutation-race-recovery-tab-mut-rem4-20260829-1338.json` returned `reconciled`, `failures: []`, and `safeForIdentityBoundCleanup: true`. All four tabs were terminal—one billed and three rejected—the floor was empty, fixture stock was exactly `63`, both the item and combo were inactive, and compatibility was version/hash `691` / `2de2c4ab0d04f2e65a4031cb987a72bc32bd5cc34af5cbadcddf45786857eb55`. Independent review then authorized one generic cleanup finalizer expected to execute no persistent action. Its first local launch was denied network access by the sandbox before any connection or staging request; the same logical identity `tab-mut-rem4-cleanup-20260829-1341` subsequently passed once with one worker and retries `0`. It revalidated the complete immutable snapshot and wrote prepared/final evidence with `actions: []`.

Independent database postflight `test-artifacts/reconciliation/checkout-tab-mutation-race-cleanup-postflight-tab-mut-rem4-cleanup-20260829-1341-tab-mut-rem4-postflight-20260829-1342.json` passed with `failures: []`, zero cleanup events/audits/mutations, the exact retained winner/loser effects, an empty floor, inactive fixtures, and unchanged compatibility version/hash `691` / `2de2c4ab0d04f2e65a4031cb987a72bc32bd5cc34af5cbadcddf45786857eb55`. The temporary admin was then deactivated, its ignored credential file removed, and no password printed; immutable lifecycle evidence is `test-artifacts/account-lifecycle/session-item-race-admin-deactivate-tab-mut-rem4-admin-20260829-1336.json`.

Independent final review issued GO to close the remaining-four subgate. Combined with the prior one-case and seven-case reconciliations, all twelve customer-tab `add_item`, `update_item`, `remove_item`, and `apply_combo` checkout-first, mutation-first, and simultaneous orderings are now closed by immutable staging evidence. Production was not accessed and remains NO-GO pending the write-off and void financial-writer races, consolidated complex UI/payment/discount/carryover acceptance, mixed representative performance and deployed query-plan/error evidence, and final independent release sign-off.
