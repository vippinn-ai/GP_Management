# Remaining Release B gap audit — 2026-08-28

## Decision

Release B remains production **NO-GO**. The financial-writer concurrency matrix, direct live session-item subgate, all twelve customer-tab item/combo orderings, payments/previous-dues, pricing/zero boundaries, and replacement/downstream/carryover UI parity are closed. Two release-gate families remain open: mixed representative performance plus deployed query-plan/error evidence, and independent final release sign-off.

This audit is read-only. It does not authorize production access or represent staging evidence for a case that has not run.

## Financial writer-concurrency map

| Required concurrent writer | Current evidence | Status |
| --- | --- | --- |
| Admin inventory metadata save | Admin-first `20260820161556`; checkout-first `20260824084853` | Closed |
| Reject session | Reject-first `20260824092648`; both checkout/reject orderings are recorded in the Release B evidence | Closed |
| Hop session | Authoritative-admin three-ordering run plus the six ordinary-role direction/order cases ending in `role_matrix_final_20260825_2249` | Closed |
| Standalone settlement of a pending bill also selected by checkout | `20260827115031`, exact before/after cleanup reconciliation | Closed |
| Bill replacement sharing limited inventory | `replace-race-20260828074932` | Closed |
| Bill refund sharing limited inventory | `refund-race-20260828082130` | Closed |
| Live repeat-combo mutation | Combined runs ending in `20260828-combo-race-ui-fix-014747` | Closed |
| Direct live session-item mutation | Checkout-first runs `session-item-race-202608280924` and `session-item-race-202608280936`; remaining-two pass `session-item-race-rem2-202608280950` | Closed |
| Direct live customer-tab item add | `tab-mut-race-20260828-1755` plus `tab-mut-rem11-20260829-1305` prove all three orderings | Closed |
| Direct live customer-tab item quantity update | `tab-mut-rem11-20260829-1305` proves checkout-first, mutation-first, and simultaneous | Closed |
| Direct live customer-tab item removal | `tab-mut-rem11-20260829-1305` plus `tab-mut-rem4-20260829-1338` prove all three orderings | Closed |
| Direct live customer-tab combo application | `tab-mut-rem4-20260829-1338` proves checkout-first, mutation-first, and simultaneous with exact application/item snapshots | Closed |
| Pending bad-debt write-off sharing the pending bill selected during checkout | `writeoff-race-20260829-1424`, `writeoff-rem2-20260829-1445`, and `writeoff-sim-20260829-1457` prove checkout-first, write-off-first, and simultaneous | Closed |
| Issued-bill void sharing a non-reusable inventory row with checkout | `void-race-20260829-2027` proves both commands commit once with exact `-1/-1/+1` stock arithmetic | Closed |

The SQL confirms these are distinct contracts. Each customer-tab mutation locks the source tab and then follows its own item/combo validation and write path; checkout independently locks the same tab before validating the canonical source items and combo snapshots. Release A behavior/realtime evidence is not concurrency evidence. `writeOffPendingBills` locks the expected pending bill, produces no payment or stock movement, and must conflict with checkout settlement if the bill changes first. `voidBill` locks the issued bill, derives reversal deltas from canonical bill lines, locks affected inventory rows, and uses the same positive `void_refund_reversal` stock contract as refund.

## Required next reusable harnesses

### Checkout versus live customer-tab item/combo mutation

The fail-closed parameterized harness, immutable recovery, identity-bound cleanup, partial-cleanup recovery, item-only continuation, and independent postflight are implemented. Runs through `tab-mut-rem11-20260829-1305` prove eight exact orderings. The remaining-eleven run timed out after seven complete cases and while only preparing the eighth; that prepared entry is not treated as a race result. Fresh run `tab-mut-rem4-20260829-1338` independently executed and reconciled exactly the remaining four orderings. All twelve customer-tab source-mutation orderings are therefore closed.

- The allowed mutation modes SHALL be exactly `add_item | update_item | remove_item | apply_combo`. Preflight, runner, browser checkpoints, reconciliation, recovery cleanup, and postflight SHALL bind the same exact ordered mode selection.
- Every mode SHALL run three separately identified, zero-retry orderings: checkout-first, mutation-first, and simultaneous. Capture the checkout and the mode-specific UI-generated RPC once in independent authenticated contexts and submit each command exactly once.
- Use run-bound catalog fixtures and a fresh customer tab per ordering. Add/update/remove SHALL exercise `add_customer_tab_item`, `update_customer_tab_item_quantity`, and `remove_customer_tab_item` respectively. Combo SHALL exercise `apply_customer_tab_combo`, including its application row and included item snapshots.
- A checkout winner SHALL create exactly one canonical bill/payment/mutation/event, close the tab, and make the stale operational command fail `customer_tab_not_open` with zero mode-specific row/audit/event effect.
- A mutation winner SHALL create exactly the mode-specific row and event effects: add/remove/apply-combo include their exact client audit row, while quantity update creates no audit row. None of the four operational RPCs may create a stock movement or alter physical stock. Their inventory effect SHALL instead be proved as the exact logical open-tab reservation delta derived from canonical `customer_tab_items` (add/apply increase it, update changes it by the quantity delta, and remove decreases it). The stale checkout SHALL fail `source_item_mismatch`, retain a null financial mutation, create no bill/payment/financial event, and leave the tab open until identity-bound rejection cleanup.
- A simultaneous ordering may produce only one of those two complete outcomes. Reconcile exact command cardinality, actors, tab status, item/combo rows, reservation/stock arithmetic, bills, payments, mutations, events, audits, and unchanged financial `app_state` compatibility. Only acknowledged fixture administration or recovery rejection may advance compatibility.
- Discovery and read-only preflight SHALL pass before any staging write. Any ambiguity stops without retry and requires the exact reconciler. The final postflight SHALL prove every run tab closed, the catalog fixtures inactive, reservations zero, and the floor empty.

### Checkout versus pending write-off

Extend the proven checkout-versus-settlement framework through an exact `writeoff` mode; do not create an unguarded duplicate runner.

- A read-only preflight SHALL bind a fresh run ID, exact staging host/project and bundle hash, active authoritative actors, empty floor, run-specific names/numbers, and exact `app_state` version/hash into an immutable `wx` artifact.
- The runner SHALL accept only full settlement discovery/execution or exact write-off discovery/execution modes and SHALL overwrite, not trust, any externally supplied mode variable.
- The browser case SHALL create one real deferred pending bill, start a second session for the same customer, select that pending bill for settlement during checkout, prepare the Bill Register's `Write Off` command in the independent context, capture each UI-generated command once, and submit each once with zero retries.
- Run three separately identified zero-retry orderings: checkout-first, write-off-first, and simultaneous. This SHALL prove both deterministic winner directions plus one true concurrent result; no ordering may be inferred from another.
- Exactly one command may commit in each ordering. A checkout winner SHALL create one current bill, one settlement payment, and leave the pending bill issued/paid while write-off returns `financial_adjustment_conflict` with a null loser mutation. A write-off winner SHALL leave the pending bill voided with reason/actor/audit/event, zero settlement payment, no checkout bill, a null checkout mutation, and checkout shall return `settlement_conflict`.
- The financial race SHALL leave `app_state` unchanged. If write-off wins, a separately identified recovery cleanup may reject only the exact unbilled second session. Every outcome SHALL end with immutable database reconciliation and an empty-floor postflight.

Execution status on 2026-08-29: `checkout_first` is closed from consumed zero-retry run `writeoff-race-20260829-1424`. Exact reconciliation revision `audit-normalization-1` is `partial` with `failures: []`, proves checkout as the sole winner, the expected two Rs 30 bills/two Rs 30 payments/two billed sessions, null losing write-off mutation, zero stock, empty floor, correct actors, and unchanged `app_state` version/hash `691` / `2de2c4ab0d04f2e65a4031cb987a72bc32bd5cc34af5cbadcddf45786857eb55`. The post-outcome Playwright failure was a local UI assertion mismatch: financial rejection uses the remote-error banner, not the operational conflict queue. It is corrected locally without rerun. No cleanup was required, the temporary admin is inactive, and credentials were removed. `writeoff_first` and `simultaneous` remain not run; the write-off subgate remains open and cannot be inferred from the closed ordering.

Subsequent execution status: fail-closed remaining-two run `writeoff-rem2-20260829-1445` closed `writeoff_first`. Write-off was the sole winner, voiding Rs 30 `BILL-20260829-002` under the distinct adjustment actor; checkout returned `settlement_conflict` with null mutation and zero financial/stock effect. The post-outcome UI stop was caused by the losing checkout modal intercepting the banner Dismiss click, and `simultaneous` did not start. Actor-bound reconciliation revision `actor-bound-1` returned `partial`, `failures: []`, then exact cleanup `writeoff-rem2-cleanup-20260829-1450` rejected only the one unbilled session. Mandatory postflight returned `partial`, `failures: []`, empty floor, preserved financial evidence, and only the acknowledged rejection advanced `app_state` to v692/hash `0c2fc997a8e6668d080b98c1c92dc3864f038ebbe391e7d2f8f07291e17e90e1`. The temporary admin is inactive and credentials removed. Thus both deterministic winner directions are closed; only `simultaneous` remains open.

Final execution status: fresh simultaneous-only preflight `writeoff-sim-20260829-1457` passed with exact selector `[simultaneous]`, distinct active actors, empty floor, zero collisions, and compatibility v692/hash `0c2fc997a8e6668d080b98c1c92dc3864f038ebbe391e7d2f8f07291e17e90e1`. One one-worker, retries-zero race ran once. Write-off was the sole winner, voiding Rs 30 `BILL-20260829-003` through mutation `financial-adjustment-fba12e3d-7885-4b44-978e-aa93034f8ac8`, event `event-3a3ba5c2-eafe-4676-93c5-3ccdc7e03ae5`, and audit `audit-c246eb81-7b00-46ce-95b9-001fb56349af`; checkout returned `settlement_conflict` with null mutation and zero bill/payment/stock effect. The browser stopped only during post-outcome dismissal because the managed-session dialog remained above the remote-error banner. No race was retried. Read-only reconciliation returned `partial`, `failures: []` and one exact cleanup candidate. Cleanup `writeoff-sim-cleanup-20260829-1957` rejected only session `session-5bf2215e-93ac-4974-ad63-5fffefbbd8c4`; mandatory postflight `checkout-writeoff-race-reconciliation-writeoff-sim-20260829-1457-writeoff-sim-cleanup-20260829-1957.json` (SHA-256 `f3ca9034c8bcddbb8d9ea0b178099a2014e6f27367fd7c87b2f6b94bf34ac79a`) returned `passed`, `failures: []`, no open sessions/tabs or cleanup candidates, and compatibility v693/hash `6af03b34ea29896c9bd5c9725f03db36954080bb4b32c8f10a4b843cf9efafc4`, advanced solely by the acknowledged rejection. The temporary admin is inactive and its credential file is removed. All three pending-write-off orderings are closed; issued-bill void is the remaining financial-writer race.

### Checkout versus issued-bill void

Extend the proven shared-inventory refund framework through an exact `void` mode; retain the default refund mode unchanged.

- Preflight, runner, checkpoints, recovery, cleanup, and postflight SHALL all carry one exact disposition value from the allowed set `refund | void`; arbitrary environment injection must fail closed.
- Use one dedicated stock-2 item and two customer tabs. Issue the first one-item bill, reserve the second unit on the second tab, then capture checkout and issued-bill void once in independent authenticated contexts.
- Both commands SHALL commit once. The original bill SHALL become `voided`; the second bill SHALL remain issued. Exact movements SHALL be first sale `-1`, second sale `-1`, and void reversal `+1`, leaving physical stock `1` with no extra movement.
- Both bills, payments, lines, tabs, mutation results, audits, events, reasons, and actors SHALL be exact. The financial race must not change `app_state`; archiving only the exact fixture may advance compatibility once. Final floor and reservations SHALL be empty.

Final execution status on 2026-08-29: fresh preflight `void-race-20260829-2027` passed with exact `void` disposition, staging project/bundle identity, distinct active admins, empty floor, zero collisions, and compatibility v693/hash `6af03b34ea29896c9bd5c9725f03db36954080bb4b32c8f10a4b843cf9efafc4`. One one-worker, retries-zero Playwright race ran once and passed. Original Rs 50 bill `BILL-QA-VOID-RACE-void-race-20260829-2027-ORIGINAL` is `voided` by actor B, while checkout bill `BILL-QA-VOID-RACE-void-race-20260829-2027-CHECKOUT` remains issued by actor A. Exactly two Rs 50 cash payments remain, both tabs are closed/billed, and movements are exactly original sale `-1`, checkout sale `-1`, and void reversal `+1`, leaving stock `1`. Immutable reconciliation and final evidence returned `passed` with exact mutations, bills, lines, payments, actors, audit, event, floor, and archive state; the financial race preserved compatibility v694/hash `fa0b94c6756e185c6e19c990127265a16fc7022e3f6fb7235a8428bc7b761d82`, and only exact fixture archival advanced it to v695/hash `59d1d3d0907e2d191fdc0b6c8bb5d22f33ca8ac2b685c932c4bacaa3e3d90742`. No recovery cleanup was needed. Independent review issued GO to close this subgate. Temporary admin `void-race-admin-20260829-2026` is inactive, its credential file is removed, no password was printed, and deactivation artifact SHA-256 is `8f0003654445fcf909edfcc822422f8a04611b854b57fdfd9675e2717a31feb5`.

Both modes require independent static GO before staging execution, unique run identities, one worker, retries `0`, no trace capture, mandatory reconciliation after any ambiguity, and no production path.

## Remaining UI matrix

The existing staging evidence closes basic timed, unit-sale, and customer-tab checkout; edited timing; pause and hop behavior; direct and multi-hop carryover/double-bill prevention; standalone/variant/cigarette/combo inventory; cash/UPI/split/deferred/partial checkout; settlement, previous-dues collection, discounts/LTP/rounding/zero guards, replacement quantity decrease, refund, receipts, reports, realtime, two-context hard refresh, and normalized downstream parity. The consolidated UI acceptance matrix is closed.

Independent design review split this into three bounded reusable phases rather than one long serial browser test. This limits ambiguous cleanup and preserves already-proven evidence without rerun:

1. Payments/previous dues: independent UPI, split, and partial-deferred-plus-in-checkout-previous-dues cases.
2. Pricing/zero boundaries: combined discounts/rounding, LTP zero, manual discount-driven zero, and true zero-price pre-submit rejection.
3. Replacement/downstream parity: quantity-decreasing replacement followed by two-context hard-refresh parity across receipts and normalized downstream screens.

Phase 3 closed on 2026-08-30 from source `replacement-parity-20260830-1542`. Corrected actor-bound read-only reanalysis SHA-256 `2879af27e847c5b321037f335c5494aac4b8d9dd4749518717eaf65241a087e7` has no integrity failures or ambiguities. Fresh read-only resume `replacement-parity-resume-20260830-1852` passed under a hard-locked, deny-by-default RPC guard; terminal SHA-256 `800c046d318d039e631676cd0a6fe1a106610d9e39d5dc869445ed7c84fe2908` proves exact original/replacement/payment/stock/canonical evidence, two hard-refresh contexts, and Bill Register, receipt, Receivables, Analytics, Customers, and Inventory agreement with zero blocked mutation requests. Exact cleanup `replacement-parity-cleanup-20260830-1855` archived only the temporary item at stock 4; postflight SHA-256 `0c21bf301905fd559ef9d42e40e262be45dd98e94bfbc44eeb62f8b42d57c3d1` preserved every financial/audit/event/canonical row, left an empty floor, and advanced compatibility once to v711. Independent review returned GO/CLOSED. Earlier read-only resume stops are retained as locator/DTO diagnostics and are not counted as passing executions.

The payments phase has exact named runners for `upi`, `split`, and `partial_previous_dues`; each overwrites its case environment, discovers exactly one one-worker/retries-zero test, verifies a fresh immutable staging preflight, writes immutable captured-not-submitted and immediate-response checkpoints, and always invokes actor-bound database reconciliation. Its reconciler checks exact sessions, bills, lines, payment modes/amounts/linkage, zero discounts/stock movements, mutation results, run-scoped events/audits, actors, empty floor, and unchanged immediate financial `app_state` windows. Ambiguity remains non-retryable. A separate SHA-bound cleanup can reject only exact reconciled open sessions and its read-only postflight proves every financial projection/canonical result unchanged, exact rejection events/audits, empty floor, and one compatibility increment per acknowledged rejection.

Fresh UPI run `paymatrix-upi-20260830-1257` passed one-worker/retries-zero Playwright and immutable reconciliation: exactly one Rs 30 issued bill, one Rs 30 UPI payment, one linked session line, one canonical mutation/event, two server-canonical audits, correct actor, empty floor, and unchanged compatibility v695/hash `59d1d3d0907e2d191fdc0b6c8bb5d22f33ca8ac2b685c932c4bacaa3e3d90742`. Independent review closed the UPI subcase. Fresh split attempt `paymatrix-split-20260830-1301` stopped before command capture/submission because the harness expected the reciprocal UPI field to update after filling cash while the rendered total was not yet bound to that interaction. Mandatory reconciliation proved zero financial effects and exact cleanup `paymatrix-split-cleanup-20260830-1305` rejected only its QA session, advancing compatibility once to v696/hash `74af7cf837c2087b84edd75cce78e551ff81d9f1537fbcef4d7a09d0c253ef0b`. A separately preflighted `paymatrix-split2-20260830-1310` attempt also stopped before financial command capture/submission. Its screenshot showed the checkout modal had captured the old 13:08 start while the save RPC was acknowledging the edited 12:58 start; the scripted 13:07 end therefore yielded a zero preview. Reconciliation remained unambiguous with zero financial rows. SHA-bound cleanup `paymatrix-split2-cleanup-20260830-1320` rejected only session `session-ba20ce3a-2f2b-4220-a7ca-f4242004dfb7`; postflight passed with an empty floor, exact rejection actor/event/audit, zero financial effects, and one compatibility increment to v697/hash `792657af206500569a1821962fbd4ac8f6766444dc4c2b8504f9eafd98a32b12`. The reusable harness now installs an exact POST response waiter before saving, requires HTTP 200 from `save_live_session_details`, asserts the checkout's start and end values, waits for a positive rendered total, and then verifies Cash Rs 10 plus exact reciprocal UPI before installing the financial interceptor. Local TypeScript, focused contracts, discovery, and independent review pass. Fresh read-only split preflight `paymatrix-split3-20260830-1330` passed; no new split mutation has yet been authorized or executed.

The separately authorized zero-retry split run `paymatrix-split3-20260830-1330` then passed Playwright and immutable reconciliation SHA-256 `cac10df9ffe58a0ba551e299604d3f01a80414592b65c4643f415c4b75346ed8`. It produced one canonical Rs 30 issued split bill for a 9-minute session, exact Rs 10 cash and Rs 20 UPI payments, one mutation/event, two server-canonical audits, correct actor, no discounts or stock changes, and an empty floor. Immediate and final checks kept financial compatibility unchanged at v697/hash `792657af206500569a1821962fbd4ac8f6766444dc4c2b8504f9eafd98a32b12`. Independent review returned GO/CLOSED; the split subcase needs no cleanup or rerun.

Fresh partial-previous-dues attempt `paymatrix-dues1-20260830-1340` committed only its intentionally deferred source: canonical pending `BILL-20260830-003`, Rs 30 total/due, no payment, one linked 9-minute session line, exact actor/event/three audits, and unchanged financial compatibility v697/hash. The second session stopped before current command capture/submission because `readCheckoutTotal` selected both the exact `Total` row and `Total Payable Today` after the previous-due panel appeared. Immutable reconciliation had no integrity failures or ambiguity and authorized only the exact open-session cleanup. `paymatrix-dues1-cleanup-20260830-1350` rejected that sole session; mandatory postflight SHA-256 `647fa62085e2112b77b5bfaebf1d61716a307bc21eca85cf5808538dac2a3fc0` passed, preserved the pending bill/canonical mutation and every financial row exactly, returned an empty floor, and advanced compatibility once to v698/hash `4f30d39dbe36f52518dbb920b7dfc077e88bc6adf958dc484713752d744e3f8e`. The harness now locates the value from the exact `Total` label, not a prefix match; TypeScript, focused contract 5/5, and one-test discovery pass. No retry has occurred, and a new run remains separately gated.

The separately preflighted attempt `paymatrix-dues2-20260830-1400` again committed only its correct pending Rs 30 source (`BILL-20260830-004`). The current command was never prepared or submitted: the retained modal/accessibility snapshot showed the Rs 20 upfront draft and exact `Upfront Mode` combobox, but selection timed out while the controlled value transition had not made the mode actionable. Reconciliation had no integrity failures or ambiguity and identified one open-session cleanup candidate. `paymatrix-dues2-cleanup-20260830-1410` rejected only that session; mandatory postflight SHA-256 `de22e1e01517e4043dc0b01e26661f5867aba855f5a1dc6659f9a7ef9eebe4b5` passed, preserved the pending source bill/canonical mutation and all financial rows exactly, left an empty floor, and advanced compatibility once to v699/hash `695afca0f8047654c261ed7205f42b83c87cc465e3cc32704477d024535b8791`. The reusable script now explicitly blurs and verifies the Rs 20 upfront value, waits for the exact `Upfront Mode` combobox to be enabled, selects UPI, and verifies that selection before proceeding. TypeScript, focused contract 5/5, and exact discovery pass. Independent review closed the cleanup and approved the correction; no retry has occurred, and another fresh run remains separately gated.

The third exact attempt `paymatrix-dues3-20260830-1430` again committed only the correct pending Rs 30 source (`BILL-20260830-005`). Its current command was never prepared or submitted because the label-based `Previous Dues Payment` selector did not bind stably to the rendered combobox. Immutable reconciliation had no integrity failure or ambiguity. Cleanup `paymatrix-dues3-cleanup-20260830-1440` rejected only the second session; postflight SHA-256 `120736240d05f67c1e248a48e3f06940e4a591b6c1fcfd1f8fc677a5b231a73d` passed, preserved the source canonical chain and zero payments, returned an empty floor, and advanced compatibility once to v700/hash `ce90bc58c6887be3561e8e784c026903adca1d8ea7791fc92be49fae895e431c`. The remaining previous-dues controls are now hardened together: exact accessible combobox/textbox roles, asserted split selection, and blur/value checks for the exact Rs 10 cash and remaining UPI amounts. Local TypeScript, focused contract 5/5, and one-test discovery pass. No retry has occurred; another fresh run is separately gated.

Fresh zero-retry run `paymatrix-dues4-20260830-1500` then passed Playwright and immutable reconciliation SHA-256 `f4053ab7a689db04dd2f76715f6b6bee6b0be8680eca2e19fe1146edb17f0726`. Source `BILL-20260830-006` was issued deferred for Rs 30 and then fully settled during checkout `BILL-20260830-007` using exact Rs 10 cash plus Rs 20 UPI with a shared settlement group and current-bill linkage. The current Rs 30 deferred bill collected Rs 20 upfront UPI and remains correctly pending with Rs 10 due. Exactly two linked 9-minute session lines, three payments, two canonical mutations/events, seven server-canonical audits, correct issue/settle/payment actor, zero discounts/stock changes, and an empty floor were proven. Both financial windows and final reconciliation kept `app_state` v700/hash `ce90bc58c6887be3561e8e784c026903adca1d8ea7791fc92be49fae895e431c` unchanged. Independent review returned GO/CLOSED; no cleanup or rerun is needed.

The pricing/zero phase is complete across fresh, non-retried identities. `pricing-positive3-20260830-1450` produced `BILL-20260830-008` for Rs 31 from a positive Rs 33.33 subtotal with exact Rs 1 line discount, Rs 1 bill discount, and Rs -0.33 rounding; corrected immutable money-scale reanalysis SHA-256 `77e2d67f437920fba73bcebac38ee1ea427fbfb0045c80bbd7d66007641e1ba9` passed with exact canonical rows, actors, audits/events, empty floor, and unchanged financial compatibility. `pricing-ltp2-20260830-1520` passed reconciliation SHA-256 `3d2e281fbdbb971f6dc89845c1baa893cc42e6564c0cd8f01748aaaa2683d452`: solo/LTP won, the positive game charge was waived by exact reason `LTP win - game charge waived`, total/paid/due were zero, and no payment row was written. `pricing-billzero2-20260830-1545` passed reconciliation SHA-256 `24451f0c933206de8e8b40ed8c489e4226e4a6539dde275860169116e279d6ba`: its positive subtotal was fully removed by an exact 100% bill discount, total/paid/due were zero, and no payment row was written. All three financial cases preserved the immediate `app_state` version/hash and left the floor empty.

The phase also exposed a narrow production-code convergence defect before the successful pricing run: an acknowledged `save_live_session_details` edit could be overwritten locally by compact realtime before checkout because acknowledged operational mutations were re-applied only when marked non-optimistic. The correction re-applies acknowledged absolute live-detail saves for sessions and tabs while explicitly excluding additive item mutations. Unit coverage pins that boundary. Full local regression passed 48 files/506 tests, TypeScript and production build passed, and staging Worker version `077da7d6-9f7c-4ca5-9ef5-51bed632615c` deployed bundle `/assets/index-GzHnG-g2.js` with SHA-256 `817e4613aa821f94ab13009365c2a19e048162d0b51749031df77bb1e294932a`. The successful positive-discount run then proved the edited start time survived the save acknowledgement, compact realtime convergence, a hard reload, and final checkout.

Finally, `pricing-truezero-20260830-1550` passed the UI guard without issuing a financial request. Immutable reconciliation SHA-256 `9c434bf94a3dc23a14a422fb580aa3dcedbed427496b8a16c3083e1b00c9b3cc` had zero integrity/completion failures or ambiguities, zero bills/payments/discounts/stock movements/financial events, and authorized only the exact QA session rejection plus zero-price item archive. SHA-bound cleanup `pricing-truezero-cleanup-20260830-1555` passed mandatory postflight SHA-256 `4342aceff0ab43ed2926196cb35160a3ec5df90cdd0560ee35da540e3c4cfa52`: the floor is empty, the item is inactive, the exact start/reject/create/archive lifecycle and actor are present, and compatibility advanced only through the acknowledged fixture create/reject/archive operations to v706/hash `dbb4383c388109c19da67b54afbe19724dc7179bbaa07cc9e01f77dae87cf26a`. Earlier selector- or assertion-blocked identities were reconciled and cleaned without retry; they are retained as evidence, not counted as passing pricing cases. Independent review verified the immutable hashes and outcomes, confirmed the saved-start proof spans acknowledgement/realtime/hard reload/checkout, and returned GO/CLOSED for Phase B. No rerun or further cleanup is required.

## Remaining performance and deployed evidence

The corrected 50-case sequential Arcade workload is complete. The remaining technical evidence was closed on 2026-08-30 by fresh zero-retry run `mixed-20260830-2004` and the deployed read-only capture:

- aggregate SHA-256 `39ad4a4439fbbfcc3e7f4c62537200d0b76050b0e1ff46896978f840e1f07800` binds the partial-previous-dues, quantity-decreasing replacement, and simultaneous checkout-versus-repeat-combo source artifacts and reconciliations;
- database p95/max were `127.985 ms`, the combo contention winner responded in `376.374 ms`, and browser completion was `1183.537`, `1669.773`, and `1290.756 ms` respectively, all inside acceptance;
- each payment/replacement financial command and each side of the combo race was captured/submitted exactly once, and the deterministic checkout loser created no bill, payment, financial mutation, or financial event;
- the initial umbrella stopped safely before combo when no eligible existing fixture existed and did not repeat payment or replacement; a separately identity-bound item/combo fixture was created, reconciled, used once, then archived combo-first/item-second with stock 20 preserved, zero cleanup stock movement, empty floor, and compatibility v718;
- deployed evidence SHA-256 `43e8ffab19d95f812716914349890ff3bb63ee774d43ecee2f42d808beb84336` verifies installed definitions, grants, RLS, indexes, query plans, exact mutation/event samples, and no financial-v2 `app_state` reference;
- authenticated log-gate SHA-256 `fd973cc6243b613a5d5cb8a4edfcf51ac8a583f3d0ede52afb7f67a15f9cc7b7` records zero matches for SQLSTATE `57014`, `40P01`, deadlock, statement timeout, timed out, or timeout exceeded. The only target-run error row was the expected `P0001` losing checkout; an earlier `25006` read-only SQL-probe error was outside the run.

Use database/API scripts for load, reconciliation, plans, and logs. Use Playwright only where UI behavior or two-browser realtime/hard-refresh parity is part of acceptance.

## Execution order

1. Retain the completed and independently reviewed twelve-case customer-tab source-mutation evidence; do not rerun it.
2. Retain the completed write-off and issued-bill-void evidence; do not rerun either financial-writer subgate.
3. Retain the completed payments/previous-dues and pricing/zero evidence; do not rerun those cases.
4. Retain the completed replacement/downstream/carryover evidence; do not repeat its financial mutations.
5. Retain the completed mixed performance/contention, deployed-plan, and log evidence; do not rerun it.
6. Retain the independent final **STAGING RELEASE B GO**; no mandatory failed, blocked, or unexplained not-run staging case remains.
7. Seek explicit production approval only after every staging gate is independently closed.

## Final independent staging verdict

The independent testing agent verified the exact mixed aggregate, deployed evidence, log-gate, and fixture-cleanup hashes; accepted the unstarted combo leg and identity-bound one-run continuation without replaying payment/replacement; and combined them with the corrected SHA-bound 50-case workload and all previously closed functional/concurrency gates. Verdict: **STAGING RELEASE B GO**. Production remains **NO-GO / no action** until the user gives explicit approval immediately before a production deploy or write and the production-specific preflight/change controls pass.
